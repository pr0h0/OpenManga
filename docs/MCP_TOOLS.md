# MCP tool and scope catalogue

<!-- Generated by `bun scripts/mcp-docs.ts` from apps/api/src/mcp. Do not edit by hand: a test compares this
     file with the generated output. -->

How to connect, approvals, manual mode and the error model are in [MCP](MCP.md).

Every successful result is `{ ok: true, status, data?, approval?, requestId, links? }`: `status` is `completed`
(then `data` has the shape below) or `pending_approval` (then `approval` says what waits for the user). Errors are
tool results with `isError: true` and `{ ok: false, error: { code, message, status, details?, requestId } }`.

## Scopes

| Scope | Consent description | Tools |
| --- | --- | --- |
| `projects:read` | View projects and project metadata. | `list_projects`, `get_project`, `duplicate_project`, `search_project`, `get_project_checks`, `get_image` |
| `projects:write` | Change project settings, state and metadata. | `update_project`, `set_project_status`, `delete_project` |
| `projects:create` | Create new projects. | `create_project`, `duplicate_project` |
| `story:read` | Read story revisions and analyses. | `get_story`, `get_story_revision`, `get_story_analysis` |
| `story:write` | Create and edit story revisions and apply story analyses. | `save_story_revision`, `run_story_analysis`, `edit_story_analysis`, `apply_story_analysis`, `run_story_rewrite` |
| `library:read` | Read characters, locations, props, styles and references. | `list_library`, `get_library_item`, `manage_character_details`, `project_style`, `get_image` |
| `library:write` | Create and edit characters, world entities, versions, outfits, styles and references. | `apply_story_analysis`, `create_library_item`, `update_library_item`, `manage_library_version`, `manage_character_details`, `migrate_character_panels`, `manage_references`, `project_style` |
| `chapters:read` | Read chapters, scenes, beats and pages. | `list_chapters`, `get_chapter`, `get_page` |
| `chapters:write` | Create, edit and re-plan chapters, scenes and pages. | `apply_story_analysis`, `manage_chapter`, `run_chapter_plan`, `manage_scene`, `manage_page` |
| `panels:read` | Read panel specs, prompts and artwork metadata. | `list_chapter_panels`, `get_page`, `get_panel`, `manage_panel_outfits`, `get_panel_prompt`, `manage_panel_artwork`, `get_image` |
| `panels:write` | Create, edit and reorder panels, specs, outfits and lettering. | `migrate_character_panels`, `manage_page`, `manage_lettering`, `update_panel`, `manage_panel_outfits`, `prepare_page_prompts`, `manage_panel_artwork`, `run_panel_check`, `manage_panel` |
| `generations:read` | Read AI job, batch, prompt and generation status. | `list_jobs`, `get_job`, `get_manual_prompt`, `estimate_bulk_generation`, `manage_batch` |
| `generations:run` | Start, retry, answer or control AI and image generation work (may spend your provider credits). | `run_story_analysis`, `run_story_rewrite`, `manage_references`, `run_chapter_plan`, `prepare_page_prompts`, `generate_panel`, `run_panel_check`, `submit_manual_answer`, `control_job`, `run_bulk_generation`, `manage_batch`, `generate_cover`, `run_narration_generation` |
| `narration:read` | Read narration text, segments, audio status and timelines. | `get_chapter_narration`, `get_narration_status` |
| `narration:write` | Edit narration and request synthesis. | `edit_narration`, `run_narration_generation`, `synthesize_narration` |
| `exports:read` | Read export status and files. | `list_exports` |
| `exports:create` | Queue project exports. | `create_export` |
| `experts:use` | Read and use your expert chats. | `list_experts`, `manage_expert_chat`, `send_expert_message`, `answer_expert_reply`, `retry_expert_reply` |
| `usage:read` | Read usage and cost information. | `get_project_usage` |

`get_server_info`, `get_answer_schema`, `describe_api` and `get_approval_request` (for the connection's own
requests) need no scope.

## Tools

| Tool | Sensitivity | Scopes |
| --- | --- | --- |
| [`get_server_info`](#get_server_info) | read | — |
| [`get_answer_schema`](#get_answer_schema) | read | — |
| [`describe_api`](#describe_api) | read | — |
| [`get_approval_request`](#get_approval_request) | read | — |
| [`list_projects`](#list_projects) | read | `projects:read` |
| [`get_project`](#get_project) | read | `projects:read` |
| [`create_project`](#create_project) | write | `projects:create` |
| [`update_project`](#update_project) | sensitive-write | `projects:write` |
| [`set_project_status`](#set_project_status) | delete | `projects:write` |
| [`delete_project`](#delete_project) | delete | `projects:write` |
| [`duplicate_project`](#duplicate_project) | sensitive-write | `projects:read` `projects:create` |
| [`search_project`](#search_project) | read | `projects:read` |
| [`get_project_checks`](#get_project_checks) | read | `projects:read` |
| [`get_project_usage`](#get_project_usage) | read | `usage:read` |
| [`get_story`](#get_story) | read | `story:read` |
| [`get_story_revision`](#get_story_revision) | read | `story:read` |
| [`save_story_revision`](#save_story_revision) | write | `story:write` |
| [`run_story_analysis`](#run_story_analysis) | spend | `story:write` `generations:run` |
| [`get_story_analysis`](#get_story_analysis) | read | `story:read` |
| [`edit_story_analysis`](#edit_story_analysis) | write | `story:write` |
| [`apply_story_analysis`](#apply_story_analysis) | sensitive-write | `story:write` `library:write` `chapters:write` |
| [`run_story_rewrite`](#run_story_rewrite) | spend | `story:write` `generations:run` |
| [`list_library`](#list_library) | read | `library:read` |
| [`get_library_item`](#get_library_item) | read | `library:read` |
| [`create_library_item`](#create_library_item) | write | `library:write` |
| [`update_library_item`](#update_library_item) | delete | `library:write` |
| [`manage_library_version`](#manage_library_version) | delete | `library:write` |
| [`manage_character_details`](#manage_character_details) | delete | `library:read` `library:write` |
| [`migrate_character_panels`](#migrate_character_panels) | sensitive-write | `library:write` `panels:write` |
| [`manage_references`](#manage_references) | spend | `library:write` `generations:run` |
| [`project_style`](#project_style) | sensitive-write | `library:read` `library:write` |
| [`list_chapters`](#list_chapters) | read | `chapters:read` |
| [`get_chapter`](#get_chapter) | read | `chapters:read` |
| [`manage_chapter`](#manage_chapter) | delete | `chapters:write` |
| [`run_chapter_plan`](#run_chapter_plan) | spend | `chapters:write` `generations:run` |
| [`manage_scene`](#manage_scene) | delete | `chapters:write` |
| [`list_chapter_panels`](#list_chapter_panels) | read | `panels:read` |
| [`get_page`](#get_page) | read | `chapters:read` `panels:read` |
| [`manage_page`](#manage_page) | delete | `chapters:write` `panels:write` |
| [`manage_lettering`](#manage_lettering) | sensitive-write | `panels:write` |
| [`get_panel`](#get_panel) | read | `panels:read` |
| [`update_panel`](#update_panel) | sensitive-write | `panels:write` |
| [`manage_panel_outfits`](#manage_panel_outfits) | write | `panels:read` `panels:write` |
| [`get_panel_prompt`](#get_panel_prompt) | read | `panels:read` |
| [`prepare_page_prompts`](#prepare_page_prompts) | spend | `panels:write` `generations:run` |
| [`generate_panel`](#generate_panel) | spend | `generations:run` |
| [`manage_panel_artwork`](#manage_panel_artwork) | delete | `panels:read` `panels:write` |
| [`run_panel_check`](#run_panel_check) | spend | `panels:write` `generations:run` |
| [`manage_panel`](#manage_panel) | delete | `panels:write` |
| [`get_image`](#get_image) | read | `panels:read` `library:read` `projects:read` |
| [`list_jobs`](#list_jobs) | read | `generations:read` |
| [`get_job`](#get_job) | read | `generations:read` |
| [`get_manual_prompt`](#get_manual_prompt) | read | `generations:read` |
| [`submit_manual_answer`](#submit_manual_answer) | write | `generations:run` |
| [`control_job`](#control_job) | spend | `generations:run` |
| [`estimate_bulk_generation`](#estimate_bulk_generation) | read | `generations:read` |
| [`run_bulk_generation`](#run_bulk_generation) | spend | `generations:run` |
| [`manage_batch`](#manage_batch) | spend | `generations:read` `generations:run` |
| [`generate_cover`](#generate_cover) | spend | `generations:run` |
| [`get_chapter_narration`](#get_chapter_narration) | read | `narration:read` |
| [`get_narration_status`](#get_narration_status) | read | `narration:read` |
| [`edit_narration`](#edit_narration) | delete | `narration:write` |
| [`run_narration_generation`](#run_narration_generation) | spend | `narration:write` `generations:run` |
| [`synthesize_narration`](#synthesize_narration) | spend | `narration:write` |
| [`create_export`](#create_export) | sensitive-write | `exports:create` |
| [`list_exports`](#list_exports) | read | `exports:read` |
| [`list_experts`](#list_experts) | read | `experts:use` |
| [`manage_expert_chat`](#manage_expert_chat) | delete | `experts:use` |
| [`send_expert_message`](#send_expert_message) | spend | `experts:use` |
| [`answer_expert_reply`](#answer_expert_reply) | write | `experts:use` |
| [`retry_expert_reply`](#retry_expert_reply) | spend | `experts:use` |

### get_server_info

OpenManga version and build, MCP protocol versions, answer schema names, limits, this connection's granted scopes/projects/approval mode, and the user's saved AI provider keys (ids and kinds only, never secrets) to use in `ai` arguments. Call this first in a session. Read-only.

- **Scopes:** none (any authenticated connection)
- **Sensitivity:** read
- **Idempotent:** yes
- **Annotations:** readOnly=true, destructive=false, idempotent=true, openWorld=false

- **Wraps:** `GET /api/meta`, `GET /api/ai/options`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {}
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {},
  "additionalProperties": {}
}
```

</details>

### get_answer_schema

The live JSON Schema of one answer format a manual (paste-mode) job can ask for, with every field explained, a commented TypeScript interface and a complete valid example. Always from this server's running version. Read-only. get_manual_prompt already includes the format for the exact question being asked; use this to prepare in advance.

- **Scopes:** none (any authenticated connection)
- **Sensitivity:** read
- **Idempotent:** yes
- **Annotations:** readOnly=true, destructive=false, idempotent=true, openWorld=false

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "enum": [
        "StoryAnalysis",
        "StoryRewrite",
        "ChapterOutline",
        "ScenePages",
        "PanelPromptDraft",
        "NarrationDraft",
        "ImageDescription",
        "PanelCheck"
      ]
    }
  },
  "required": [
    "name"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "name": {
      "type": "string"
    },
    "askedBy": {
      "type": "string"
    },
    "jsonSchema": {
      "type": "object",
      "propertyNames": {
        "type": "string"
      },
      "additionalProperties": {}
    },
    "interface": {
      "type": "string"
    },
    "example": {}
  },
  "required": [
    "name",
    "askedBy",
    "jsonSchema",
    "interface",
    "example"
  ],
  "additionalProperties": {}
}
```

</details>

### describe_api

Search OpenManga's REST API description (from the live OpenAPI registry) to understand what an operation does and what it accepts. Returns at most 20 matching operations, with schemas only when `includeSchemas` is true. Informational: MCP tools are how you act; there is no generic REST call tool. Read-only.

- **Scopes:** none (any authenticated connection)
- **Sensitivity:** read
- **Idempotent:** yes
- **Annotations:** readOnly=true, destructive=false, idempotent=true, openWorld=false

- **Wraps:** `GET /api/docs/openapi.json`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "tag": {
      "description": "e.g. projects, stories, chapters, panels, generations, narration",
      "type": "string",
      "maxLength": 40
    },
    "path": {
      "description": "Substring of the path, e.g. /chapters/",
      "type": "string",
      "maxLength": 200
    },
    "method": {
      "type": "string",
      "enum": [
        "GET",
        "POST",
        "PATCH",
        "PUT",
        "DELETE"
      ]
    },
    "query": {
      "description": "Words to find in the summary",
      "type": "string",
      "maxLength": 100
    },
    "includeSchemas": {
      "default": false,
      "type": "boolean"
    }
  }
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "operations": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {},
        "additionalProperties": {}
      }
    },
    "total": {
      "type": "number"
    },
    "tags": {
      "type": "array",
      "items": {
        "type": "string"
      }
    }
  },
  "required": [
    "operations",
    "total",
    "tags"
  ]
}
```

</details>

### get_approval_request

Check an approval request this connection created (the approvalRequestId from a pending_approval result). Returns pending, executed (with the original tool's saved result — continue from it, do not call the original tool again), denied, expired, stale or failed. Poll only after a reasonable delay or when the user says they decided; never in a tight loop. Read-only.

- **Scopes:** none (any authenticated connection)
- **Sensitivity:** read
- **Idempotent:** yes
- **Annotations:** readOnly=true, destructive=false, idempotent=true, openWorld=false

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "approvalRequestId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    }
  },
  "required": [
    "approvalRequestId"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {},
  "additionalProperties": {}
}
```

</details>

### list_projects

List the user's projects this connection may see, newest activity first, with chapter/panel/generation counts and spend. Paginated (default 25, max 100). Use get_project for detail. Read-only.

- **Scopes:** `projects:read`
- **Sensitivity:** read
- **Idempotent:** yes
- **Annotations:** readOnly=true, destructive=false, idempotent=true, openWorld=false

- **Wraps:** `GET /api/projects`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "status": {
      "default": "active",
      "type": "string",
      "enum": [
        "active",
        "archived",
        "trash",
        "all"
      ]
    },
    "limit": {
      "default": 25,
      "description": "Page size (max 100).",
      "type": "integer",
      "minimum": 1,
      "maximum": 100
    },
    "offset": {
      "default": 0,
      "description": "Items to skip.",
      "type": "integer",
      "minimum": 0,
      "maximum": 9007199254740991
    }
  }
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "items": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "title": {
            "type": "string"
          },
          "projectType": {
            "type": "string"
          },
          "status": {
            "type": "string"
          }
        },
        "required": [
          "id",
          "title",
          "projectType",
          "status"
        ],
        "additionalProperties": {}
      }
    },
    "total": {
      "type": "number"
    },
    "offset": {
      "type": "number"
    },
    "limit": {
      "type": "number"
    },
    "nextOffset": {
      "type": [
        "number",
        "null"
      ]
    }
  },
  "required": [
    "items",
    "total",
    "offset",
    "limit",
    "nextOffset"
  ]
}
```

</details>

### get_project

One project's overview: settings (format, language, budget, narration, lettering), counts (chapters, pages, panels, panels with art, cast, active jobs, spend) and current style. Read-only. Next: get_story, list_chapters, list_library.

- **Scopes:** `projects:read`
- **Sensitivity:** read
- **Idempotent:** yes
- **Annotations:** readOnly=true, destructive=false, idempotent=true, openWorld=false

- **Wraps:** `GET /api/projects/:projectId`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "projectId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    }
  },
  "required": [
    "projectId"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {},
  "additionalProperties": {}
}
```

</details>

### create_project

Create a new project (optionally with its first story revision in `story`). Needs the connection's permission to create projects; a connection limited to selected projects is granted the new one automatically. `format`: comic pages, vertical strip, or film (16:9 shots). New projects start with a $5 budget cap. Not asynchronous. Next: save_story_revision / run_story_analysis.

- **Scopes:** `projects:create`
- **Sensitivity:** write (the most sensitive action; each call is classified by what it does)
- **Idempotent:** no — accepts `idempotencyKey`
- **Annotations:** readOnly=false, destructive=false, idempotent=false, openWorld=false
- **Approval action keys:** `project.create`
- **Wraps:** `POST /api/projects`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "title": {
      "type": "string",
      "minLength": 1,
      "maxLength": 200
    },
    "description": {
      "default": "",
      "type": "string",
      "maxLength": 5000
    },
    "projectType": {
      "default": "manhwa",
      "type": "string",
      "enum": [
        "manga",
        "manhwa",
        "webtoon",
        "comic",
        "illustrated_story"
      ]
    },
    "language": {
      "default": "en",
      "type": "string",
      "minLength": 2,
      "maxLength": 16
    },
    "readingDirection": {
      "type": "string",
      "enum": [
        "ltr",
        "rtl",
        "vertical"
      ]
    },
    "colorMode": {
      "default": "full_color",
      "type": "string",
      "enum": [
        "full_color",
        "grayscale",
        "bw_manga"
      ]
    },
    "stylePresetKey": {
      "type": "string",
      "maxLength": 64
    },
    "customStyle": {
      "default": "",
      "type": "string",
      "maxLength": 4000
    },
    "format": {
      "default": "comic",
      "type": "string",
      "enum": [
        "comic",
        "film",
        "vertical"
      ]
    },
    "story": {
      "type": "object",
      "properties": {
        "content": {
          "type": "string",
          "minLength": 1,
          "maxLength": 500000
        },
        "inputKind": {
          "default": "story",
          "type": "string",
          "enum": [
            "story",
            "chapter",
            "outline",
            "screenplay",
            "idea"
          ]
        },
        "title": {
          "default": "",
          "type": "string",
          "maxLength": 200
        }
      },
      "required": [
        "content"
      ]
    },
    "idempotencyKey": {
      "description": "Optional client request id. Retrying with the same key and arguments returns the first result instead of repeating the action; the same key with different arguments is a conflict.",
      "type": "string",
      "minLength": 8,
      "maxLength": 128,
      "pattern": "^[\\w.:-]+$"
    }
  },
  "required": [
    "title"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "project": {
      "type": "object",
      "properties": {},
      "additionalProperties": {}
    }
  },
  "required": [
    "project"
  ]
}
```

</details>

### update_project

Change a project's title, description, type, language, reading direction, colour mode or `settings` (merged into the current settings: budgetUsd, narration voice/speed, lettering defaults, imageQuality, ...). Raising or clearing the budget cap is sensitive and may need the user's approval. The format cannot change once pages exist.

- **Scopes:** `projects:write`
- **Sensitivity:** sensitive-write (the most sensitive action; each call is classified by what it does)
- **Idempotent:** yes
- **Annotations:** readOnly=false, destructive=true, idempotent=true, openWorld=false
- **Approval action keys:** `project.update`, `project.budget`
- **Wraps:** `PATCH /api/projects/:projectId`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "title": {
      "type": "string",
      "minLength": 1,
      "maxLength": 200
    },
    "description": {
      "type": "string",
      "maxLength": 5000
    },
    "projectType": {
      "type": "string",
      "enum": [
        "manga",
        "manhwa",
        "webtoon",
        "comic",
        "illustrated_story"
      ]
    },
    "language": {
      "type": "string",
      "minLength": 2,
      "maxLength": 16
    },
    "readingDirection": {
      "type": "string",
      "enum": [
        "ltr",
        "rtl",
        "vertical"
      ]
    },
    "colorMode": {
      "type": "string",
      "enum": [
        "full_color",
        "grayscale",
        "bw_manga"
      ]
    },
    "settings": {
      "type": "object",
      "properties": {
        "format": {
          "type": "string",
          "enum": [
            "comic",
            "film",
            "vertical"
          ]
        },
        "pageWidth": {
          "type": "integer",
          "minimum": 256,
          "maximum": 8000
        },
        "pageHeight": {
          "type": "integer",
          "minimum": 256,
          "maximum": 20000
        },
        "pageGutter": {
          "type": "number",
          "minimum": 0,
          "maximum": 0.1
        },
        "pageMargin": {
          "type": "number",
          "minimum": 0,
          "maximum": 0.2
        },
        "imageQuality": {
          "type": "string",
          "enum": [
            "low",
            "medium",
            "high"
          ]
        },
        "narrationVoice": {
          "type": "string"
        },
        "narrationSpeed": {
          "type": "number",
          "minimum": 0.5,
          "maximum": 2
        },
        "narrationWordsPerPanel": {
          "type": "integer",
          "minimum": 5,
          "maximum": 80
        },
        "referenceMaxWidth": {
          "type": "integer",
          "minimum": 16,
          "maximum": 2048
        },
        "referenceMaxHeight": {
          "type": "integer",
          "minimum": 16,
          "maximum": 2048
        },
        "webtoonGap": {
          "type": "integer",
          "minimum": 0,
          "maximum": 1000
        },
        "webtoonChunkHeight": {
          "type": "integer",
          "minimum": 1000,
          "maximum": 40000
        },
        "webtoonWidth": {
          "type": "integer",
          "minimum": 320,
          "maximum": 2000
        },
        "worldNotes": {
          "type": "string"
        },
        "author": {
          "type": "string"
        },
        "lettering": {
          "type": "object",
          "properties": {
            "autoPlace": {
              "type": "boolean"
            },
            "autoFit": {
              "type": "boolean"
            },
            "maxWidth": {
              "type": "number",
              "minimum": 0.1,
              "maximum": 1
            },
            "types": {
              "type": "object",
              "properties": {
                "normal": {
                  "type": "object",
                  "properties": {
                    "font": {
                      "type": "string",
                      "minLength": 1,
                      "maxLength": 64
                    },
                    "fontSize": {
                      "type": "number",
                      "minimum": 4,
                      "maximum": 200
                    },
                    "lineHeight": {
                      "type": "number",
                      "minimum": 0.8,
                      "maximum": 3
                    },
                    "align": {
                      "type": "string",
                      "enum": [
                        "left",
                        "center",
                        "right"
                      ]
                    },
                    "padding": {
                      "type": "number",
                      "minimum": 0,
                      "maximum": 200
                    },
                    "background": {
                      "type": "string",
                      "maxLength": 32
                    },
                    "textColor": {
                      "type": "string",
                      "maxLength": 32
                    },
                    "borderColor": {
                      "type": "string",
                      "maxLength": 32
                    },
                    "borderWidth": {
                      "type": "number",
                      "minimum": 0,
                      "maximum": 20
                    }
                  }
                },
                "thought": {
                  "type": "object",
                  "properties": {
                    "font": {
                      "type": "string",
                      "minLength": 1,
                      "maxLength": 64
                    },
                    "fontSize": {
                      "type": "number",
                      "minimum": 4,
                      "maximum": 200
                    },
                    "lineHeight": {
                      "type": "number",
                      "minimum": 0.8,
                      "maximum": 3
                    },
                    "align": {
                      "type": "string",
                      "enum": [
                        "left",
                        "center",
                        "right"
                      ]
                    },
                    "padding": {
                      "type": "number",
                      "minimum": 0,
                      "maximum": 200
                    },
                    "background": {
                      "type": "string",
                      "maxLength": 32
                    },
                    "textColor": {
                      "type": "string",
                      "maxLength": 32
                    },
                    "borderColor": {
                      "type": "string",
                      "maxLength": 32
                    },
                    "borderWidth": {
                      "type": "number",
                      "minimum": 0,
                      "maximum": 20
                    }
                  }
                },
                "shout": {
                  "type": "object",
                  "properties": {
                    "font": {
                      "type": "string",
                      "minLength": 1,
                      "maxLength": 64
                    },
                    "fontSize": {
                      "type": "number",
                      "minimum": 4,
                      "maximum": 200
                    },
                    "lineHeight": {
                      "type": "number",
                      "minimum": 0.8,
                      "maximum": 3
                    },
                    "align": {
                      "type": "string",
                      "enum": [
                        "left",
                        "center",
                        "right"
                      ]
                    },
                    "padding": {
                      "type": "number",
                      "minimum": 0,
                      "maximum": 200
                    },
                    "background": {
                      "type": "string",
                      "maxLength": 32
                    },
                    "textColor": {
                      "type": "string",
                      "maxLength": 32
                    },
                    "borderColor": {
                      "type": "string",
                      "maxLength": 32
                    },
                    "borderWidth": {
                      "type": "number",
                      "minimum": 0,
                      "maximum": 20
                    }
                  }
                },
                "whisper": {
                  "type": "object",
                  "properties": {
                    "font": {
                      "type": "string",
                      "minLength": 1,
                      "maxLength": 64
                    },
                    "fontSize": {
                      "type": "number",
                      "minimum": 4,
                      "maximum": 200
                    },
                    "lineHeight": {
                      "type": "number",
                      "minimum": 0.8,
                      "maximum": 3
                    },
                    "align": {
                      "type": "string",
                      "enum": [
                        "left",
                        "center",
                        "right"
                      ]
                    },
                    "padding": {
                      "type": "number",
                      "minimum": 0,
                      "maximum": 200
                    },
                    "background": {
                      "type": "string",
                      "maxLength": 32
                    },
                    "textColor": {
                      "type": "string",
                      "maxLength": 32
                    },
                    "borderColor": {
                      "type": "string",
                      "maxLength": 32
                    },
                    "borderWidth": {
                      "type": "number",
                      "minimum": 0,
                      "maximum": 20
                    }
                  }
                },
                "narration": {
                  "type": "object",
                  "properties": {
                    "font": {
                      "type": "string",
                      "minLength": 1,
                      "maxLength": 64
                    },
                    "fontSize": {
                      "type": "number",
                      "minimum": 4,
                      "maximum": 200
                    },
                    "lineHeight": {
                      "type": "number",
                      "minimum": 0.8,
                      "maximum": 3
                    },
                    "align": {
                      "type": "string",
                      "enum": [
                        "left",
                        "center",
                        "right"
                      ]
                    },
                    "padding": {
                      "type": "number",
                      "minimum": 0,
                      "maximum": 200
                    },
                    "background": {
                      "type": "string",
                      "maxLength": 32
                    },
                    "textColor": {
                      "type": "string",
                      "maxLength": 32
                    },
                    "borderColor": {
                      "type": "string",
                      "maxLength": 32
                    },
                    "borderWidth": {
                      "type": "number",
                      "minimum": 0,
                      "maximum": 20
                    }
                  }
                },
                "system": {
                  "type": "object",
                  "properties": {
                    "font": {
                      "type": "string",
                      "minLength": 1,
                      "maxLength": 64
                    },
                    "fontSize": {
                      "type": "number",
                      "minimum": 4,
                      "maximum": 200
                    },
                    "lineHeight": {
                      "type": "number",
                      "minimum": 0.8,
                      "maximum": 3
                    },
                    "align": {
                      "type": "string",
                      "enum": [
                        "left",
                        "center",
                        "right"
                      ]
                    },
                    "padding": {
                      "type": "number",
                      "minimum": 0,
                      "maximum": 200
                    },
                    "background": {
                      "type": "string",
                      "maxLength": 32
                    },
                    "textColor": {
                      "type": "string",
                      "maxLength": 32
                    },
                    "borderColor": {
                      "type": "string",
                      "maxLength": 32
                    },
                    "borderWidth": {
                      "type": "number",
                      "minimum": 0,
                      "maximum": 20
                    }
                  }
                }
              }
            },
            "sfx": {
              "type": "object",
              "properties": {
                "font": {
                  "type": "string",
                  "minLength": 1,
                  "maxLength": 64
                },
                "fontSize": {
                  "type": "number",
                  "minimum": 8,
                  "maximum": 400
                },
                "fill": {
                  "type": "string",
                  "maxLength": 32
                },
                "stroke": {
                  "type": "string",
                  "maxLength": 32
                },
                "strokeWidth": {
                  "type": "number",
                  "minimum": 0,
                  "maximum": 30
                },
                "opacity": {
                  "type": "number",
                  "minimum": 0,
                  "maximum": 1
                }
              }
            }
          }
        },
        "budgetUsd": {
          "anyOf": [
            {
              "type": "number",
              "minimum": 0,
              "maximum": 1000000
            },
            {
              "type": "null"
            }
          ]
        },
        "contentPolicyFallback": {
          "type": "object",
          "properties": {
            "enabled": {
              "default": false,
              "type": "boolean"
            },
            "credentialId": {
              "default": null,
              "anyOf": [
                {
                  "type": "string",
                  "format": "uuid",
                  "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
                },
                {
                  "type": "null"
                }
              ]
            },
            "provider": {
              "default": null,
              "anyOf": [
                {
                  "type": "string",
                  "maxLength": 40
                },
                {
                  "type": "null"
                }
              ]
            },
            "model": {
              "default": "",
              "type": "string",
              "maxLength": 200
            }
          }
        },
        "narrationPauseMs": {
          "type": "integer",
          "minimum": 0,
          "maximum": 5000
        },
        "sceneBreakPauseMs": {
          "type": "integer",
          "minimum": 0,
          "maximum": 10000
        },
        "narrationStyle": {
          "type": "string",
          "maxLength": 500
        },
        "consistencyCheck": {
          "type": "object",
          "properties": {
            "enabled": {
              "default": false,
              "type": "boolean"
            },
            "credentialId": {
              "default": null,
              "anyOf": [
                {
                  "type": "string",
                  "format": "uuid",
                  "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
                },
                {
                  "type": "null"
                }
              ]
            },
            "model": {
              "default": "",
              "type": "string",
              "maxLength": 200
            }
          }
        }
      }
    },
    "projectId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    }
  },
  "required": [
    "projectId"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "project": {
      "type": "object",
      "properties": {},
      "additionalProperties": {}
    }
  },
  "required": [
    "project"
  ]
}
```

</details>

### set_project_status

archive / unarchive a project, move it to trash, or restore it from trash. Trash removes it from normal use (delete class: may need approval). Permanent deletion is delete_project.

- **Scopes:** `projects:write`
- **Sensitivity:** delete (the most sensitive action; each call is classified by what it does)
- **Idempotent:** yes
- **Annotations:** readOnly=false, destructive=true, idempotent=true, openWorld=false
- **Approval action keys:** `project.archive`, `project.unarchive`, `project.trash`, `project.restore`
- **Wraps:** `POST /api/projects/:projectId/status`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "projectId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "action": {
      "type": "string",
      "enum": [
        "archive",
        "unarchive",
        "trash",
        "restore"
      ]
    }
  },
  "required": [
    "projectId",
    "action"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "project": {
      "type": "object",
      "properties": {},
      "additionalProperties": {}
    }
  },
  "required": [
    "project"
  ]
}
```

</details>

### delete_project

PERMANENTLY delete a project that is already in trash, with all its files. Cannot be undone. Always a delete-class action (may need the user's approval).

- **Scopes:** `projects:write`
- **Sensitivity:** delete (the most sensitive action; each call is classified by what it does)
- **Idempotent:** no
- **Annotations:** readOnly=false, destructive=true, idempotent=false, openWorld=false
- **Approval action keys:** `project.delete`
- **Wraps:** `DELETE /api/projects/:projectId`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "projectId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    }
  },
  "required": [
    "projectId"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "ok": {
      "type": "boolean"
    }
  },
  "required": [
    "ok"
  ]
}
```

</details>

### duplicate_project

Deep-copy a project (story, cast, chapters, pages, panels, artwork references) into a new project. Needs permission to create projects; sensitive (may need approval). The copy is granted to this connection.

- **Scopes:** `projects:read`, `projects:create`
- **Sensitivity:** sensitive-write (the most sensitive action; each call is classified by what it does)
- **Idempotent:** no — accepts `idempotencyKey`
- **Annotations:** readOnly=false, destructive=true, idempotent=false, openWorld=false
- **Approval action keys:** `project.duplicate`
- **Wraps:** `POST /api/projects/:projectId/duplicate`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "projectId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "idempotencyKey": {
      "description": "Optional client request id. Retrying with the same key and arguments returns the first result instead of repeating the action; the same key with different arguments is a conflict.",
      "type": "string",
      "minLength": 8,
      "maxLength": 128,
      "pattern": "^[\\w.:-]+$"
    }
  },
  "required": [
    "projectId"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "project": {
      "type": "object",
      "properties": {},
      "additionalProperties": {}
    }
  },
  "required": [
    "project"
  ]
}
```

</details>

### search_project

Search one project's characters, places, props, chapters, scenes and panels by text. Read-only.

- **Scopes:** `projects:read`
- **Sensitivity:** read
- **Idempotent:** yes
- **Annotations:** readOnly=true, destructive=false, idempotent=true, openWorld=false

- **Wraps:** `GET /api/projects/:projectId/search`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "projectId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "q": {
      "type": "string",
      "minLength": 1,
      "maxLength": 100
    }
  },
  "required": [
    "projectId",
    "q"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {},
  "additionalProperties": {}
}
```

</details>

### get_project_checks

check=readiness: what an export would lack (missing artwork, narration coverage and audio, draft or superseded versions) and whether the user has a usable provider key. check=preflight: a dry run before image generation (risky vocabulary, stale or missing references, lighting issues). Optionally limited to a chapter (or a page for preflight). Read-only; run before bulk generation or export.

- **Scopes:** `projects:read`
- **Sensitivity:** read
- **Idempotent:** yes
- **Annotations:** readOnly=true, destructive=false, idempotent=true, openWorld=false

- **Wraps:** `GET /api/projects/:projectId/readiness`, `GET /api/projects/:projectId/preflight`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "projectId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "check": {
      "type": "string",
      "enum": [
        "readiness",
        "preflight"
      ]
    },
    "chapterId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "pageId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "language": {
      "type": "string",
      "maxLength": 16
    }
  },
  "required": [
    "projectId",
    "check"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {},
  "additionalProperties": {}
}
```

</details>

### get_project_usage

A project's AI usage and estimated cost breakdown (by kind, provider and model) and its budget. Read-only.

- **Scopes:** `usage:read`
- **Sensitivity:** read
- **Idempotent:** yes
- **Annotations:** readOnly=true, destructive=false, idempotent=true, openWorld=false

- **Wraps:** `GET /api/projects/:projectId/usage`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "projectId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    }
  },
  "required": [
    "projectId"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {},
  "additionalProperties": {}
}
```

</details>

### get_story

A project's story revisions (metadata and length, newest first) and its analyses (status and ids, without their full results). Read the text itself with get_story_revision and an analysis with get_story_analysis. Read-only.

- **Scopes:** `story:read`
- **Sensitivity:** read
- **Idempotent:** yes
- **Annotations:** readOnly=true, destructive=false, idempotent=true, openWorld=false

- **Wraps:** `GET /api/projects/:projectId/story`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "projectId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    }
  },
  "required": [
    "projectId"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "revisions": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {},
        "additionalProperties": {}
      }
    },
    "analyses": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {},
        "additionalProperties": {}
      }
    },
    "latestRevisionId": {
      "type": [
        "string",
        "null"
      ]
    }
  },
  "required": [
    "revisions",
    "analyses",
    "latestRevisionId"
  ]
}
```

</details>

### get_story_revision

The text of one story revision, in chunks of up to 50000 characters (use offset to read on). Read-only.

- **Scopes:** `story:read`
- **Sensitivity:** read
- **Idempotent:** yes
- **Annotations:** readOnly=true, destructive=false, idempotent=true, openWorld=false

- **Wraps:** `GET /api/story-revisions/:id`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "revisionId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "offset": {
      "default": 0,
      "type": "integer",
      "minimum": 0,
      "maximum": 9007199254740991
    }
  },
  "required": [
    "revisionId"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {},
  "additionalProperties": {}
}
```

</details>

### save_story_revision

Create a new story revision in a project (projectId), or edit an existing one (revisionId). Editing a revision that is locked (it was analysed) forks a new revision instead of overwriting it; the result says `forked`. Pass baseSha256 from the revision you read to refuse overwriting someone else's newer edit. Not asynchronous. Next: run_story_analysis.

- **Scopes:** `story:write`
- **Sensitivity:** write (the most sensitive action; each call is classified by what it does)
- **Idempotent:** no — accepts `idempotencyKey`
- **Annotations:** readOnly=false, destructive=false, idempotent=false, openWorld=false
- **Approval action keys:** `story_revision.create`, `story_revision.edit`
- **Wraps:** `POST /api/projects/:projectId/story/revisions`, `PATCH /api/story-revisions/:id`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "projectId": {
      "description": "Create a new revision in this project.",
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "revisionId": {
      "description": "Edit this revision instead.",
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "content": {
      "type": "string",
      "minLength": 1,
      "maxLength": 500000
    },
    "title": {
      "default": "",
      "type": "string",
      "maxLength": 200
    },
    "inputKind": {
      "type": "string",
      "enum": [
        "story",
        "chapter",
        "outline",
        "screenplay",
        "idea"
      ]
    },
    "baseSha256": {
      "type": "string"
    },
    "idempotencyKey": {
      "description": "Optional client request id. Retrying with the same key and arguments returns the first result instead of repeating the action; the same key with different arguments is a conflict.",
      "type": "string",
      "minLength": 8,
      "maxLength": 128,
      "pattern": "^[\\w.:-]+$"
    }
  }
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "revision": {
      "type": "object",
      "properties": {},
      "additionalProperties": {}
    }
  },
  "required": [
    "revision"
  ],
  "additionalProperties": {}
}
```

</details>

### run_story_analysis

Queue a story analysis of a revision: it proposes the cast, locations, props and chapters (a StoryAnalysis) and locks the revision. Asynchronous: returns a job immediately; poll get_job. With ai.manual=true (no provider key needed, no spending) the job becomes awaiting_input: get_manual_prompt, answer with a StoryAnalysis JSON via submit_manual_answer. With a provider it spends the user's credits (may need approval). When the job completes, review with get_story_analysis, optionally edit_story_analysis, then apply_story_analysis.

- **Scopes:** `story:write`, `generations:run`
- **Sensitivity:** spend (the most sensitive action; each call is classified by what it does)
- **Idempotent:** no — accepts `idempotencyKey`
- **Annotations:** readOnly=false, destructive=false, idempotent=false, openWorld=false
- **Approval action keys:** `story.analyze`
- **Wraps:** `POST /api/story-revisions/:id/analyze`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "revisionId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "ai": {
      "type": "object",
      "properties": {
        "manual": {
          "description": "Paste mode: the job compiles its prompt and waits for your answer (get_manual_prompt). No spending.",
          "type": "boolean"
        },
        "credentialId": {
          "description": "One of the user's saved provider keys (ids from get_server_info).",
          "type": "string",
          "format": "uuid",
          "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
        },
        "provider": {
          "description": "Use the user's first saved key for this provider kind.",
          "type": "string",
          "maxLength": 40
        },
        "model": {
          "description": "Model id; defaults to the provider's first model.",
          "type": "string",
          "maxLength": 200
        }
      }
    },
    "batch": {
      "type": "boolean"
    },
    "idempotencyKey": {
      "description": "Optional client request id. Retrying with the same key and arguments returns the first result instead of repeating the action; the same key with different arguments is a conflict.",
      "type": "string",
      "minLength": 8,
      "maxLength": 128,
      "pattern": "^[\\w.:-]+$"
    }
  },
  "required": [
    "revisionId"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "analysis": {
      "type": "object",
      "properties": {},
      "additionalProperties": {}
    },
    "job": {
      "type": "object",
      "properties": {},
      "additionalProperties": {}
    }
  },
  "required": [
    "analysis",
    "job"
  ]
}
```

</details>

### get_story_analysis

One story analysis with its full result (the proposed cast, world and chapters) once it has completed. Read-only.

- **Scopes:** `story:read`
- **Sensitivity:** read
- **Idempotent:** yes
- **Annotations:** readOnly=true, destructive=false, idempotent=true, openWorld=false

- **Wraps:** `GET /api/story-analyses/:id`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "analysisId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    }
  },
  "required": [
    "analysisId"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "analysis": {
      "type": "object",
      "properties": {},
      "additionalProperties": {}
    }
  },
  "required": [
    "analysis"
  ]
}
```

</details>

### edit_story_analysis

Replace a completed analysis's result before applying it (the whole StoryAnalysis object; see get_answer_schema StoryAnalysis). Validated exactly as a provider's answer is. Does not create anything yet; apply_story_analysis does.

- **Scopes:** `story:write`
- **Sensitivity:** write (the most sensitive action; each call is classified by what it does)
- **Idempotent:** yes
- **Annotations:** readOnly=false, destructive=false, idempotent=true, openWorld=false
- **Approval action keys:** `story_analysis.edit`
- **Wraps:** `PATCH /api/story-analyses/:id`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "analysisId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "result": {
      "type": "object",
      "propertyNames": {
        "type": "string"
      },
      "additionalProperties": {},
      "description": "The complete StoryAnalysis object (get_answer_schema StoryAnalysis)."
    }
  },
  "required": [
    "analysisId",
    "result"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "analysis": {
      "type": "object",
      "properties": {},
      "additionalProperties": {}
    }
  },
  "required": [
    "analysis"
  ]
}
```

</details>

### apply_story_analysis

Create the cast (characters with versions and outfits), locations, props and chapters proposed by a completed analysis. Creates many entities at once, so it is sensitive (may need the user's approval). Not asynchronous. Next: list_chapters, then run_chapter_plan per chapter.

- **Scopes:** `story:write`, `library:write`, `chapters:write`
- **Sensitivity:** sensitive-write (the most sensitive action; each call is classified by what it does)
- **Idempotent:** no — accepts `idempotencyKey`
- **Annotations:** readOnly=false, destructive=true, idempotent=false, openWorld=false
- **Approval action keys:** `story_analysis.apply`
- **Wraps:** `POST /api/story-analyses/:id/apply`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "analysisId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "idempotencyKey": {
      "description": "Optional client request id. Retrying with the same key and arguments returns the first result instead of repeating the action; the same key with different arguments is a conflict.",
      "type": "string",
      "minLength": 8,
      "maxLength": 128,
      "pattern": "^[\\w.:-]+$"
    }
  },
  "required": [
    "analysisId"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "created": {
      "type": "object",
      "properties": {},
      "additionalProperties": {}
    }
  },
  "required": [
    "created"
  ]
}
```

</details>

### run_story_rewrite

Queue an AI rewrite of a revision following `instruction`; the result is a new revision (the original is kept). Asynchronous: returns a job; poll get_job. Manual mode (ai.manual=true) asks you for a StoryRewrite answer via get_manual_prompt / submit_manual_answer; a provider run spends credits (may need approval).

- **Scopes:** `story:write`, `generations:run`
- **Sensitivity:** spend (the most sensitive action; each call is classified by what it does)
- **Idempotent:** no — accepts `idempotencyKey`
- **Annotations:** readOnly=false, destructive=false, idempotent=false, openWorld=false
- **Approval action keys:** `story.rewrite`
- **Wraps:** `POST /api/story-revisions/:id/rewrite`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "revisionId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "instruction": {
      "type": "string",
      "minLength": 3,
      "maxLength": 4000
    },
    "ai": {
      "type": "object",
      "properties": {
        "manual": {
          "description": "Paste mode: the job compiles its prompt and waits for your answer (get_manual_prompt). No spending.",
          "type": "boolean"
        },
        "credentialId": {
          "description": "One of the user's saved provider keys (ids from get_server_info).",
          "type": "string",
          "format": "uuid",
          "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
        },
        "provider": {
          "description": "Use the user's first saved key for this provider kind.",
          "type": "string",
          "maxLength": 40
        },
        "model": {
          "description": "Model id; defaults to the provider's first model.",
          "type": "string",
          "maxLength": 200
        }
      }
    },
    "batch": {
      "type": "boolean"
    },
    "idempotencyKey": {
      "description": "Optional client request id. Retrying with the same key and arguments returns the first result instead of repeating the action; the same key with different arguments is a conflict.",
      "type": "string",
      "minLength": 8,
      "maxLength": 128,
      "pattern": "^[\\w.:-]+$"
    }
  },
  "required": [
    "revisionId",
    "instruction"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "job": {
      "type": "object",
      "properties": {},
      "additionalProperties": {}
    }
  },
  "required": [
    "job"
  ],
  "additionalProperties": {}
}
```

</details>

### list_library

kind=characters: the cast with current version, status and reference readiness. kind=locations / props: the world entities. kind=styles: the project's style versions with references. kind=style_presets: built-in presets. Read-only.

- **Scopes:** `library:read`
- **Sensitivity:** read
- **Idempotent:** yes
- **Annotations:** readOnly=true, destructive=false, idempotent=true, openWorld=false

- **Wraps:** `GET /api/projects/:projectId/characters`, `GET /api/projects/:projectId/locations`, `GET /api/projects/:projectId/props`, `GET /api/projects/:projectId/style`, `GET /api/style-presets`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "projectId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "kind": {
      "type": "string",
      "enum": [
        "characters",
        "locations",
        "props",
        "styles",
        "style_presets"
      ]
    },
    "trash": {
      "default": false,
      "description": "Characters only: list trashed ones instead.",
      "type": "boolean"
    }
  },
  "required": [
    "projectId",
    "kind"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {},
  "additionalProperties": {}
}
```

</details>

### get_library_item

One character, location or prop with all its versions (draft / approved / locked / superseded), references and, for characters, aliases and outfits. Read-only.

- **Scopes:** `library:read`
- **Sensitivity:** read
- **Idempotent:** yes
- **Annotations:** readOnly=true, destructive=false, idempotent=true, openWorld=false

- **Wraps:** `GET /api/characters/:id`, `GET /api/locations/:id`, `GET /api/props/:id`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "kind": {
      "type": "string",
      "enum": [
        "character",
        "location",
        "prop"
      ]
    },
    "id": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    }
  },
  "required": [
    "kind",
    "id"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {},
  "additionalProperties": {}
}
```

</details>

### create_library_item

Add a character (name, role, description = visual bible fields, aliases), location or prop (name, description) to a project, with a first draft version. Normal write. The description fields are the same as get_answer_schema StoryAnalysis explains for characters/locations/props.

- **Scopes:** `library:write`
- **Sensitivity:** write (the most sensitive action; each call is classified by what it does)
- **Idempotent:** no — accepts `idempotencyKey`
- **Annotations:** readOnly=false, destructive=false, idempotent=false, openWorld=false
- **Approval action keys:** `character.create`, `location.create`, `prop.create`
- **Wraps:** `POST /api/projects/:projectId/characters`, `POST /api/projects/:projectId/locations`, `POST /api/projects/:projectId/props`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "projectId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "kind": {
      "type": "string",
      "enum": [
        "character",
        "location",
        "prop"
      ]
    },
    "name": {
      "type": "string",
      "minLength": 1,
      "maxLength": 120
    },
    "role": {
      "default": "supporting",
      "type": "string",
      "enum": [
        "protagonist",
        "antagonist",
        "supporting",
        "minor"
      ]
    },
    "description": {
      "default": {},
      "type": "object",
      "propertyNames": {
        "type": "string"
      },
      "additionalProperties": {}
    },
    "aliases": {
      "default": [],
      "maxItems": 50,
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1,
        "maxLength": 120
      }
    },
    "idempotencyKey": {
      "description": "Optional client request id. Retrying with the same key and arguments returns the first result instead of repeating the action; the same key with different arguments is a conflict.",
      "type": "string",
      "minLength": 8,
      "maxLength": 128,
      "pattern": "^[\\w.:-]+$"
    }
  },
  "required": [
    "projectId",
    "kind",
    "name"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {},
  "additionalProperties": {}
}
```

</details>

### update_library_item

update: rename (and for characters change role). switch_version: make another version the current one (new panels and generation use it; existing panels keep theirs) — sensitive. trash / restore: move to or back from trash (trash is delete class).

- **Scopes:** `library:write`
- **Sensitivity:** delete (the most sensitive action; each call is classified by what it does)
- **Idempotent:** yes
- **Annotations:** readOnly=false, destructive=true, idempotent=true, openWorld=false
- **Approval action keys:** `character.update`, `character.switch_version`, `character.trash`, `character.restore`, `location.*`, `prop.*`
- **Wraps:** `PATCH /api/characters/:id`, `PATCH /api/locations/:id`, `PATCH /api/props/:id`, `DELETE /api/characters/:id`, `POST /api/characters/:id/restore`, `DELETE /api/locations/:id`, `POST /api/locations/:id/restore`, `DELETE /api/props/:id`, `POST /api/props/:id/restore`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "kind": {
      "type": "string",
      "enum": [
        "character",
        "location",
        "prop"
      ]
    },
    "id": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "action": {
      "type": "string",
      "enum": [
        "update",
        "switch_version",
        "trash",
        "restore"
      ]
    },
    "name": {
      "type": "string",
      "minLength": 1,
      "maxLength": 120
    },
    "role": {
      "default": "supporting",
      "type": "string",
      "enum": [
        "protagonist",
        "antagonist",
        "supporting",
        "minor"
      ]
    },
    "currentVersionId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    }
  },
  "required": [
    "kind",
    "id",
    "action"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {},
  "additionalProperties": {}
}
```

</details>

### manage_library_version

Approved and locked versions are immutable: to change one, create a new version (it starts as a draft copy of the current one), edit the draft, then approve it. create: new version (makeCurrent=true also switches the entity to it: sensitive). edit: change a DRAFT version's description (characters also immutableTraits, changeNote). set_status: approve / lock / supersede (sensitive; locking also locks its approved references). delete: delete a draft version no panel uses (delete class).

- **Scopes:** `library:write`
- **Sensitivity:** delete (the most sensitive action; each call is classified by what it does)
- **Idempotent:** no
- **Annotations:** readOnly=false, destructive=true, idempotent=false, openWorld=false
- **Approval action keys:** `<kind>_version.create`, `<kind>_version.create_current`, `<kind>_version.edit`, `<kind>_version.<status>`, `<kind>_version.delete`
- **Wraps:** `POST /api/characters/:id/versions`, `PATCH /api/character-versions/:id`, `POST /api/character-versions/:id/status`, `DELETE /api/character-versions/:id`, `POST /api/locations/:id/versions`, `PATCH /api/location-versions/:id`, `POST /api/location-versions/:id/status`, `POST /api/props/:id/versions`, `PATCH /api/prop-versions/:id`, `POST /api/prop-versions/:id/status`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "kind": {
      "type": "string",
      "enum": [
        "character",
        "location",
        "prop"
      ]
    },
    "action": {
      "type": "string",
      "enum": [
        "create",
        "edit",
        "set_status",
        "delete"
      ]
    },
    "entityId": {
      "description": "For create.",
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "versionId": {
      "description": "For edit, set_status, delete.",
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "fromVersionId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "makeCurrent": {
      "default": true,
      "type": "boolean"
    },
    "description": {
      "type": "object",
      "propertyNames": {
        "type": "string"
      },
      "additionalProperties": {}
    },
    "immutableTraits": {
      "maxItems": 40,
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1,
        "maxLength": 200
      }
    },
    "changeNote": {
      "type": "string",
      "maxLength": 500
    },
    "status": {
      "type": "string",
      "enum": [
        "draft",
        "approved",
        "locked",
        "superseded"
      ]
    }
  },
  "required": [
    "kind",
    "action"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {},
  "additionalProperties": {}
}
```

</details>

### manage_character_details

add_alias / remove_alias: names the story also uses for a character. add_outfit / update_outfit / delete_outfit: outfit variants (name, description, isDefault, optionally tied to a version). outfit_timeline: every outfit change of the character in reading order (read-only). Normal writes; delete_outfit is delete class.

- **Scopes:** `library:read`, `library:write` — per action, see description
- **Sensitivity:** delete (the most sensitive action; each call is classified by what it does)
- **Idempotent:** no
- **Annotations:** readOnly=false, destructive=true, idempotent=false, openWorld=false
- **Approval action keys:** `character.alias`, `character.outfit`, `character.outfit_delete`
- **Wraps:** `POST /api/characters/:id/aliases`, `DELETE /api/character-aliases/:id`, `POST /api/characters/:id/outfits`, `PATCH /api/character-outfits/:id`, `DELETE /api/character-outfits/:id`, `GET /api/characters/:id/outfit-timeline`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "action": {
      "type": "string",
      "enum": [
        "add_alias",
        "remove_alias",
        "add_outfit",
        "update_outfit",
        "delete_outfit",
        "outfit_timeline"
      ]
    },
    "characterId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "aliasId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "alias": {
      "type": "string",
      "minLength": 1,
      "maxLength": 120
    },
    "outfitId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "outfit": {
      "description": "add_outfit needs name; update_outfit changes only the fields sent.",
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "minLength": 1,
          "maxLength": 120
        },
        "description": {
          "type": "string",
          "maxLength": 2000
        },
        "isDefault": {
          "type": "boolean"
        },
        "characterVersionId": {
          "anyOf": [
            {
              "type": "string",
              "format": "uuid",
              "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    }
  },
  "required": [
    "action"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {},
  "additionalProperties": {}
}
```

</details>

### migrate_character_panels

Explicitly move panels (all of them, or panelIds) from one version of a character to another, so they are drawn with the new appearance on their next generation. Refused when the target version has no approved reference unless force=true (panels then lose identity pinning). Sensitive: may need approval. Nothing is regenerated.

- **Scopes:** `library:write`, `panels:write`
- **Sensitivity:** sensitive-write (the most sensitive action; each call is classified by what it does)
- **Idempotent:** yes
- **Annotations:** readOnly=false, destructive=true, idempotent=true, openWorld=false
- **Approval action keys:** `character_version.migrate_panels`
- **Wraps:** `POST /api/characters/:id/migrate-panels`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "fromVersionId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "toVersionId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "panelIds": {
      "type": "array",
      "items": {
        "type": "string",
        "format": "uuid",
        "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
      }
    },
    "force": {
      "default": false,
      "type": "boolean"
    },
    "characterId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    }
  },
  "required": [
    "fromVersionId",
    "toVersionId",
    "characterId"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {},
  "additionalProperties": {}
}
```

</details>

### manage_references

generate: queue a reference image for a character/location/prop version or a project style (kind: portrait, full_body, multi-angle and the other reference kinds; optional outfit for characters). Spends image-provider credits (may need approval); asynchronous, returns a job. set_status: approve / lock / unapprove a reference (approval makes it the identity reference sent with panel generation; sensitive). make_primary: make it the version's primary reference (sensitive). trash: move it to trash (delete class). Uploading your own reference needs the OpenManga UI.

- **Scopes:** `library:write`, `generations:run` — per action, see description
- **Sensitivity:** spend (the most sensitive action; each call is classified by what it does)
- **Idempotent:** no — accepts `idempotencyKey`
- **Annotations:** readOnly=false, destructive=false, idempotent=false, openWorld=false
- **Approval action keys:** `reference.generate`, `reference.<status>`, `reference.primary`, `reference.trash`
- **Wraps:** `POST /api/character-versions/:id/references/generate`, `POST /api/location-versions/:id/references/generate`, `POST /api/prop-versions/:id/references/generate`, `POST /api/project-styles/:id/references/generate`, `POST /api/references/:id/status`, `POST /api/references/:id/primary`, `DELETE /api/references/:id`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "action": {
      "type": "string",
      "enum": [
        "generate",
        "set_status",
        "make_primary",
        "trash"
      ]
    },
    "subject": {
      "description": "For generate.",
      "type": "string",
      "enum": [
        "character",
        "location",
        "prop",
        "style"
      ]
    },
    "versionId": {
      "description": "For generate: the version (or project style) id.",
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "referenceId": {
      "description": "For set_status, make_primary, trash.",
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "generate": {
      "type": "object",
      "properties": {
        "kind": {
          "default": "full_body",
          "type": "string",
          "enum": [
            "portrait",
            "full_body",
            "multi_angle",
            "expression_sheet",
            "outfit",
            "location",
            "location_panorama",
            "location_sheet",
            "prop",
            "prop_multi_angle",
            "style"
          ]
        },
        "extraInstruction": {
          "type": "string",
          "maxLength": 2000
        },
        "outfitId": {
          "type": "string",
          "format": "uuid",
          "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
        }
      }
    },
    "status": {
      "type": "string",
      "enum": [
        "draft",
        "approved",
        "locked",
        "superseded"
      ]
    },
    "ai": {
      "type": "object",
      "properties": {
        "credentialId": {
          "description": "One of the user's saved provider keys (ids from get_server_info).",
          "type": "string",
          "format": "uuid",
          "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
        },
        "provider": {
          "description": "Use the user's first saved key for this provider kind.",
          "type": "string",
          "maxLength": 40
        },
        "model": {
          "type": "string",
          "maxLength": 200
        }
      }
    },
    "idempotencyKey": {
      "description": "Optional client request id. Retrying with the same key and arguments returns the first result instead of repeating the action; the same key with different arguments is a conflict.",
      "type": "string",
      "minLength": 8,
      "maxLength": 128,
      "pattern": "^[\\w.:-]+$"
    }
  },
  "required": [
    "action"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {},
  "additionalProperties": {}
}
```

</details>

### project_style

get: the project's style history with references. set: a new style version from a preset key and/or a custom description/definition; it becomes current, so all later generation uses it (sensitive). make_current: switch back to an earlier style version (sensitive).

- **Scopes:** `library:read`, `library:write` — per action, see description
- **Sensitivity:** sensitive-write (the most sensitive action; each call is classified by what it does)
- **Idempotent:** no
- **Annotations:** readOnly=false, destructive=true, idempotent=false, openWorld=false
- **Approval action keys:** `style.set`, `style.make_current`
- **Wraps:** `GET /api/projects/:projectId/style`, `POST /api/projects/:projectId/style`, `POST /api/project-styles/:id/make-current`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "projectId": {
      "description": "For get and set.",
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "action": {
      "type": "string",
      "enum": [
        "get",
        "set",
        "make_current"
      ]
    },
    "style": {
      "type": "object",
      "properties": {
        "stylePresetKey": {
          "anyOf": [
            {
              "type": "string",
              "maxLength": 64
            },
            {
              "type": "null"
            }
          ]
        },
        "customDescription": {
          "default": "",
          "type": "string",
          "maxLength": 4000
        },
        "customDefinition": {
          "type": "object",
          "properties": {
            "summary": {
              "default": "",
              "type": "string"
            },
            "lineTreatment": {
              "default": "",
              "type": "string"
            },
            "colorPolicy": {
              "default": "",
              "type": "string"
            },
            "shading": {
              "default": "",
              "type": "string"
            },
            "detailLevel": {
              "default": "",
              "type": "string"
            },
            "faceRendering": {
              "default": "",
              "type": "string"
            },
            "backgroundRendering": {
              "default": "",
              "type": "string"
            },
            "motionEffects": {
              "default": "",
              "type": "string"
            },
            "contrast": {
              "default": "",
              "type": "string"
            },
            "screenTones": {
              "default": "",
              "type": "string"
            },
            "lighting": {
              "default": "",
              "type": "string"
            },
            "exclusions": {
              "default": [],
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          }
        }
      },
      "required": [
        "stylePresetKey"
      ]
    },
    "styleId": {
      "description": "For make_current.",
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    }
  },
  "required": [
    "action"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {},
  "additionalProperties": {}
}
```

</details>

### list_chapters

A project's chapters in order, with scene/page/panel counts, how many panels have artwork, narration coverage and whether a plan exists. Read-only. Work chapter by chapter: get_chapter, then run_chapter_plan.

- **Scopes:** `chapters:read`
- **Sensitivity:** read
- **Idempotent:** yes
- **Annotations:** readOnly=true, destructive=false, idempotent=true, openWorld=false

- **Wraps:** `GET /api/projects/:projectId/chapters`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "projectId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    }
  },
  "required": [
    "projectId"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "chapters": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {},
        "additionalProperties": {}
      }
    }
  },
  "required": [
    "chapters"
  ]
}
```

</details>

### get_chapter

One chapter: memory (summary, opening/closing state, continuity), scenes with their beats, and pages with panel counts. The source excerpt is cut to 2000 characters unless includeSource=true. Panels are listed by list_chapter_panels (paginated) and in detail by get_page / get_panel. Read-only.

- **Scopes:** `chapters:read`
- **Sensitivity:** read
- **Idempotent:** yes
- **Annotations:** readOnly=true, destructive=false, idempotent=true, openWorld=false

- **Wraps:** `GET /api/chapters/:id`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "chapterId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "includeSource": {
      "default": false,
      "type": "boolean"
    }
  },
  "required": [
    "chapterId"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "chapter": {
      "type": "object",
      "properties": {},
      "additionalProperties": {}
    },
    "scenes": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {},
        "additionalProperties": {}
      }
    },
    "pages": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {},
        "additionalProperties": {}
      }
    }
  },
  "required": [
    "chapter",
    "scenes",
    "pages"
  ],
  "additionalProperties": {}
}
```

</details>

### manage_chapter

create: add a chapter to a project (title, summary, sourceExcerpt: the story text this chapter covers). update: edit its fields and chapter memory, reorder it, or set planStatus (approve/lock the plan). delete: remove the chapter with its pages and panels (generated images stay in the asset library) — delete class, may need approval.

- **Scopes:** `chapters:write`
- **Sensitivity:** delete (the most sensitive action; each call is classified by what it does)
- **Idempotent:** no — accepts `idempotencyKey`
- **Annotations:** readOnly=false, destructive=true, idempotent=false, openWorld=false
- **Approval action keys:** `chapter.create`, `chapter.update`, `chapter.delete`
- **Wraps:** `POST /api/projects/:projectId/chapters`, `PATCH /api/chapters/:id`, `DELETE /api/chapters/:id`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "action": {
      "type": "string",
      "enum": [
        "create",
        "update",
        "delete"
      ]
    },
    "projectId": {
      "description": "For create.",
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "chapterId": {
      "description": "For update and delete.",
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "fields": {
      "description": "create: title (required), summary, sourceExcerpt. update: any.",
      "type": "object",
      "properties": {
        "title": {
          "type": "string",
          "minLength": 1,
          "maxLength": 200
        },
        "summary": {
          "type": "string",
          "maxLength": 10000
        },
        "sourceExcerpt": {
          "type": "string",
          "maxLength": 500000
        },
        "openingState": {
          "type": "string",
          "maxLength": 10000
        },
        "closingState": {
          "type": "string",
          "maxLength": 10000
        },
        "characterStateChanges": {
          "maxItems": 100,
          "type": "array",
          "items": {
            "type": "string",
            "maxLength": 1000
          }
        },
        "locationStateChanges": {
          "maxItems": 100,
          "type": "array",
          "items": {
            "type": "string",
            "maxLength": 1000
          }
        },
        "revealedFacts": {
          "maxItems": 100,
          "type": "array",
          "items": {
            "type": "string",
            "maxLength": 1000
          }
        },
        "order": {
          "type": "integer",
          "minimum": 1,
          "maximum": 9007199254740991
        },
        "planStatus": {
          "type": "string",
          "enum": [
            "draft",
            "approved",
            "locked",
            "superseded"
          ]
        }
      }
    },
    "idempotencyKey": {
      "description": "Optional client request id. Retrying with the same key and arguments returns the first result instead of repeating the action; the same key with different arguments is a conflict.",
      "type": "string",
      "minLength": 8,
      "maxLength": 128,
      "pattern": "^[\\w.:-]+$"
    }
  },
  "required": [
    "action"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {},
  "additionalProperties": {}
}
```

</details>

### run_chapter_plan

Queue planning of a chapter into scenes, pages and panels (for a film project: shots; for a vertical strip: one panel per page). Asynchronous: returns a job; poll get_job. With ai.manual=true (no key, no spending) the job asks several questions in turn: first a ChapterOutline, then one ScenePages per scene — each time it is awaiting_input, call get_manual_prompt and submit_manual_answer, until the job completes. A chapter that already has pages needs replace=true, which replaces them (sensitive; may need approval). A provider run spends credits (may need approval). One plan per chapter at a time.

- **Scopes:** `chapters:write`, `generations:run`
- **Sensitivity:** spend (the most sensitive action; each call is classified by what it does)
- **Idempotent:** no — accepts `idempotencyKey`
- **Annotations:** readOnly=false, destructive=false, idempotent=false, openWorld=false
- **Approval action keys:** `chapter.plan`, `chapter.replan`
- **Wraps:** `POST /api/chapters/:id/plan`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "chapterId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "replace": {
      "default": false,
      "type": "boolean"
    },
    "targetPages": {
      "type": "integer",
      "minimum": 1,
      "maximum": 60
    },
    "ai": {
      "type": "object",
      "properties": {
        "manual": {
          "description": "Paste mode: the job compiles its prompt and waits for your answer (get_manual_prompt). No spending.",
          "type": "boolean"
        },
        "credentialId": {
          "description": "One of the user's saved provider keys (ids from get_server_info).",
          "type": "string",
          "format": "uuid",
          "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
        },
        "provider": {
          "description": "Use the user's first saved key for this provider kind.",
          "type": "string",
          "maxLength": 40
        },
        "model": {
          "description": "Model id; defaults to the provider's first model.",
          "type": "string",
          "maxLength": 200
        }
      }
    },
    "batch": {
      "type": "boolean"
    },
    "idempotencyKey": {
      "description": "Optional client request id. Retrying with the same key and arguments returns the first result instead of repeating the action; the same key with different arguments is a conflict.",
      "type": "string",
      "minLength": 8,
      "maxLength": 128,
      "pattern": "^[\\w.:-]+$"
    }
  },
  "required": [
    "chapterId"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "job": {
      "type": "object",
      "properties": {},
      "additionalProperties": {}
    }
  },
  "required": [
    "job"
  ]
}
```

</details>

### manage_scene

create: add a scene to a chapter. update: edit a scene's fields, location, cast and continuity state. replace_beats: replace a scene's story beats with the given list. delete: remove the scene (its pages stay, unassigned; delete class, may need approval).

- **Scopes:** `chapters:write`
- **Sensitivity:** delete (the most sensitive action; each call is classified by what it does)
- **Idempotent:** no
- **Annotations:** readOnly=false, destructive=true, idempotent=false, openWorld=false
- **Approval action keys:** `scene.create`, `scene.update`, `scene.beats`, `scene.delete`
- **Wraps:** `POST /api/chapters/:id/scenes`, `PATCH /api/scenes/:id`, `PUT /api/scenes/:id/beats`, `DELETE /api/scenes/:id`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "action": {
      "type": "string",
      "enum": [
        "create",
        "update",
        "replace_beats",
        "delete"
      ]
    },
    "chapterId": {
      "description": "For create.",
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "sceneId": {
      "description": "For update and replace_beats.",
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "fields": {
      "description": "create needs title.",
      "type": "object",
      "properties": {
        "title": {
          "type": "string",
          "minLength": 1,
          "maxLength": 200
        },
        "summary": {
          "type": "string",
          "maxLength": 10000
        },
        "locationId": {
          "anyOf": [
            {
              "type": "string",
              "format": "uuid",
              "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
            },
            {
              "type": "null"
            }
          ]
        },
        "time": {
          "type": "string",
          "maxLength": 200
        },
        "weather": {
          "type": "string",
          "maxLength": 200
        },
        "characterIds": {
          "maxItems": 50,
          "type": "array",
          "items": {
            "type": "string",
            "format": "uuid",
            "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
          }
        },
        "purpose": {
          "type": "string",
          "maxLength": 2000
        },
        "opening": {
          "type": "string",
          "maxLength": 2000
        },
        "progression": {
          "type": "string",
          "maxLength": 2000
        },
        "climax": {
          "type": "string",
          "maxLength": 2000
        },
        "ending": {
          "type": "string",
          "maxLength": 2000
        },
        "continuityNotes": {
          "maxItems": 100,
          "type": "array",
          "items": {
            "type": "string",
            "maxLength": 500
          }
        },
        "initialState": {
          "type": "object",
          "propertyNames": {
            "type": "string"
          },
          "additionalProperties": {
            "type": "string",
            "maxLength": 500
          }
        },
        "finalState": {
          "type": "object",
          "propertyNames": {
            "type": "string"
          },
          "additionalProperties": {
            "type": "string",
            "maxLength": 500
          }
        },
        "continuityDeltas": {
          "maxItems": 100,
          "type": "array",
          "items": {
            "type": "string",
            "maxLength": 500
          }
        }
      }
    },
    "beats": {
      "maxItems": 200,
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1,
        "maxLength": 1000
      }
    }
  },
  "required": [
    "action"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {},
  "additionalProperties": {}
}
```

</details>

### list_chapter_panels

A chapter's panels in reading order as compact summaries (page, shot, camera, story beat, cast and location version ids, status, approval, whether it has artwork, review flags). Paginated (default 25, max 100). Detail: get_panel. Read-only.

- **Scopes:** `panels:read`
- **Sensitivity:** read
- **Idempotent:** yes
- **Annotations:** readOnly=true, destructive=false, idempotent=true, openWorld=false

- **Wraps:** `GET /api/chapters/:id/panels`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "chapterId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "limit": {
      "default": 25,
      "description": "Page size (max 100).",
      "type": "integer",
      "minimum": 1,
      "maximum": 100
    },
    "offset": {
      "default": 0,
      "description": "Items to skip.",
      "type": "integer",
      "minimum": 0,
      "maximum": 9007199254740991
    }
  },
  "required": [
    "chapterId"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "items": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {},
        "additionalProperties": {}
      }
    },
    "total": {
      "type": "number"
    },
    "nextOffset": {
      "type": [
        "number",
        "null"
      ]
    }
  },
  "required": [
    "items",
    "total",
    "nextOffset"
  ],
  "additionalProperties": {}
}
```

</details>

### get_page

One page's editor document: its panels with their current specs, speech bubbles, SFX and on-page narration boxes, the chapter and neighbouring pages. Read-only.

- **Scopes:** `chapters:read`, `panels:read`
- **Sensitivity:** read
- **Idempotent:** yes
- **Annotations:** readOnly=true, destructive=false, idempotent=true, openWorld=false

- **Wraps:** `GET /api/pages/:id`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "pageId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    }
  },
  "required": [
    "pageId"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {},
  "additionalProperties": {}
}
```

</details>

### manage_page

create: add a page to a chapter with a layout template. update: edit the page plan (purpose, pacing, emphasis, hook, size, status, scene, order). set_layout: swap the layout template (keeps panels, adds empty ones if needed). add_panel: add a panel (or duplicate one). reorder_panels: set the reading order. update_document: move/resize/reorder panels and commit bubble/SFX/caption geometry in one atomic write (replaces those values; sensitive). delete: remove the page (delete class).

- **Scopes:** `chapters:write`, `panels:write` — per action, see description
- **Sensitivity:** delete (the most sensitive action; each call is classified by what it does)
- **Idempotent:** no
- **Annotations:** readOnly=false, destructive=true, idempotent=false, openWorld=false
- **Approval action keys:** `page.create`, `page.update`, `page.layout`, `panel.create`, `page.reorder`, `page.document`, `page.delete`
- **Wraps:** `POST /api/chapters/:id/pages`, `PATCH /api/pages/:id`, `POST /api/pages/:id/layout`, `POST /api/pages/:id/panels`, `POST /api/pages/:id/reorder-panels`, `PATCH /api/pages/:id/document`, `DELETE /api/pages/:id`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "action": {
      "type": "string",
      "enum": [
        "create",
        "update",
        "set_layout",
        "add_panel",
        "reorder_panels",
        "update_document",
        "delete"
      ]
    },
    "chapterId": {
      "description": "For create.",
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "pageId": {
      "description": "For every other action.",
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "create": {
      "type": "object",
      "properties": {
        "layoutTemplate": {
          "default": "four-grid",
          "type": "string",
          "maxLength": 64
        },
        "sceneId": {
          "default": null,
          "anyOf": [
            {
              "type": "string",
              "format": "uuid",
              "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
            },
            {
              "type": "null"
            }
          ]
        },
        "afterPageId": {
          "type": "string",
          "format": "uuid",
          "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
        }
      }
    },
    "update": {
      "type": "object",
      "properties": {
        "purpose": {
          "type": "string",
          "maxLength": 2000
        },
        "pacing": {
          "type": "string",
          "maxLength": 500
        },
        "visualEmphasis": {
          "type": "string",
          "maxLength": 2000
        },
        "pageTurnHook": {
          "type": "string",
          "maxLength": 2000
        },
        "width": {
          "type": "integer",
          "minimum": 256,
          "maximum": 8000
        },
        "height": {
          "type": "integer",
          "minimum": 256,
          "maximum": 20000
        },
        "readingDirection": {
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "ltr",
                "rtl",
                "vertical"
              ]
            },
            {
              "type": "null"
            }
          ]
        },
        "status": {
          "type": "string",
          "enum": [
            "draft",
            "approved",
            "locked",
            "superseded"
          ]
        },
        "sceneId": {
          "anyOf": [
            {
              "type": "string",
              "format": "uuid",
              "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
            },
            {
              "type": "null"
            }
          ]
        },
        "order": {
          "type": "integer",
          "minimum": 1,
          "maximum": 9007199254740991
        }
      }
    },
    "layoutTemplate": {
      "type": "string",
      "maxLength": 64
    },
    "addPanel": {
      "type": "object",
      "properties": {
        "frame": {
          "type": "object",
          "properties": {
            "x": {
              "type": "number",
              "minimum": 0,
              "maximum": 1
            },
            "y": {
              "type": "number",
              "minimum": 0,
              "maximum": 1
            },
            "width": {
              "type": "number",
              "exclusiveMinimum": 0,
              "maximum": 1
            },
            "height": {
              "type": "number",
              "exclusiveMinimum": 0,
              "maximum": 1
            }
          },
          "required": [
            "x",
            "y",
            "width",
            "height"
          ]
        },
        "duplicateOf": {
          "type": "string",
          "format": "uuid",
          "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
        }
      }
    },
    "panelIds": {
      "description": "For reorder_panels: every panel id, in reading order.",
      "minItems": 1,
      "maxItems": 50,
      "type": "array",
      "items": {
        "type": "string",
        "format": "uuid",
        "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
      }
    },
    "document": {
      "type": "object",
      "properties": {
        "panels": {
          "default": [],
          "maxItems": 50,
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "id": {
                "type": "string",
                "format": "uuid",
                "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
              },
              "frame": {
                "type": "object",
                "properties": {
                  "x": {
                    "type": "number",
                    "minimum": 0,
                    "maximum": 1
                  },
                  "y": {
                    "type": "number",
                    "minimum": 0,
                    "maximum": 1
                  },
                  "width": {
                    "type": "number",
                    "exclusiveMinimum": 0,
                    "maximum": 1
                  },
                  "height": {
                    "type": "number",
                    "exclusiveMinimum": 0,
                    "maximum": 1
                  }
                },
                "required": [
                  "x",
                  "y",
                  "width",
                  "height"
                ]
              },
              "imageTransform": {
                "type": "object",
                "properties": {
                  "focalX": {
                    "default": 0.5,
                    "type": "number",
                    "minimum": 0,
                    "maximum": 1
                  },
                  "focalY": {
                    "default": 0.5,
                    "type": "number",
                    "minimum": 0,
                    "maximum": 1
                  },
                  "scale": {
                    "default": 1,
                    "type": "number",
                    "minimum": 1,
                    "maximum": 8
                  }
                }
              },
              "order": {
                "type": "integer",
                "minimum": 1,
                "maximum": 9007199254740991
              }
            },
            "required": [
              "id"
            ]
          }
        },
        "dialogue": {
          "default": [],
          "maxItems": 200,
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "id": {
                "type": "string",
                "format": "uuid",
                "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
              },
              "bubble": {
                "type": "object",
                "properties": {
                  "type": {
                    "default": "normal",
                    "type": "string",
                    "enum": [
                      "normal",
                      "thought",
                      "shout",
                      "whisper",
                      "narration",
                      "system"
                    ]
                  },
                  "font": {
                    "default": "Comic Neue",
                    "type": "string"
                  },
                  "fontSize": {
                    "default": 28,
                    "type": "number",
                    "minimum": 4,
                    "maximum": 200
                  },
                  "lineHeight": {
                    "default": 1.2,
                    "type": "number",
                    "minimum": 0.8,
                    "maximum": 3
                  },
                  "align": {
                    "default": "center",
                    "type": "string",
                    "enum": [
                      "left",
                      "center",
                      "right"
                    ]
                  },
                  "padding": {
                    "default": 18,
                    "type": "number",
                    "minimum": 0,
                    "maximum": 200
                  },
                  "background": {
                    "default": "#ffffff",
                    "type": "string"
                  },
                  "textColor": {
                    "default": "#111111",
                    "type": "string"
                  },
                  "borderColor": {
                    "default": "#111111",
                    "type": "string"
                  },
                  "borderWidth": {
                    "default": 3,
                    "type": "number",
                    "minimum": 0,
                    "maximum": 20
                  },
                  "tail": {
                    "default": true,
                    "type": "boolean"
                  },
                  "tailTarget": {
                    "type": "object",
                    "properties": {
                      "x": {
                        "type": "number",
                        "minimum": 0,
                        "maximum": 1
                      },
                      "y": {
                        "type": "number",
                        "minimum": 0,
                        "maximum": 1
                      }
                    },
                    "required": [
                      "x",
                      "y"
                    ]
                  },
                  "rotation": {
                    "default": 0,
                    "type": "number",
                    "minimum": -180,
                    "maximum": 180
                  },
                  "x": {
                    "type": "number",
                    "minimum": 0,
                    "maximum": 1
                  },
                  "y": {
                    "type": "number",
                    "minimum": 0,
                    "maximum": 1
                  },
                  "width": {
                    "type": "number",
                    "exclusiveMinimum": 0,
                    "maximum": 1
                  },
                  "height": {
                    "type": "number",
                    "exclusiveMinimum": 0,
                    "maximum": 1
                  },
                  "zIndex": {
                    "default": 10,
                    "type": "integer",
                    "minimum": -9007199254740991,
                    "maximum": 9007199254740991
                  }
                },
                "required": [
                  "x",
                  "y",
                  "width",
                  "height"
                ]
              },
              "text": {
                "type": "string",
                "maxLength": 2000
              },
              "panelId": {
                "anyOf": [
                  {
                    "type": "string",
                    "format": "uuid",
                    "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
                  },
                  {
                    "type": "null"
                  }
                ]
              }
            },
            "required": [
              "id"
            ]
          }
        },
        "sfx": {
          "default": [],
          "maxItems": 200,
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "id": {
                "type": "string",
                "format": "uuid",
                "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
              },
              "style": {
                "type": "object",
                "properties": {
                  "font": {
                    "default": "DejaVu Sans",
                    "type": "string"
                  },
                  "fontSize": {
                    "default": 72,
                    "type": "number",
                    "minimum": 8,
                    "maximum": 400
                  },
                  "fill": {
                    "default": "#ffdd00",
                    "type": "string"
                  },
                  "stroke": {
                    "default": "#111111",
                    "type": "string"
                  },
                  "strokeWidth": {
                    "default": 6,
                    "type": "number",
                    "minimum": 0,
                    "maximum": 30
                  },
                  "rotation": {
                    "default": -8,
                    "type": "number",
                    "minimum": -180,
                    "maximum": 180
                  },
                  "scale": {
                    "default": 1,
                    "type": "number",
                    "minimum": 0.1,
                    "maximum": 10
                  },
                  "opacity": {
                    "default": 1,
                    "type": "number",
                    "minimum": 0,
                    "maximum": 1
                  },
                  "x": {
                    "type": "number",
                    "minimum": 0,
                    "maximum": 1
                  },
                  "y": {
                    "type": "number",
                    "minimum": 0,
                    "maximum": 1
                  },
                  "zIndex": {
                    "default": 20,
                    "type": "integer",
                    "minimum": -9007199254740991,
                    "maximum": 9007199254740991
                  }
                },
                "required": [
                  "x",
                  "y"
                ]
              },
              "text": {
                "type": "string",
                "maxLength": 100
              }
            },
            "required": [
              "id"
            ]
          }
        },
        "narration": {
          "default": [],
          "maxItems": 200,
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "id": {
                "type": "string",
                "format": "uuid",
                "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
              },
              "box": {
                "anyOf": [
                  {
                    "type": "object",
                    "properties": {
                      "type": {
                        "default": "normal",
                        "type": "string",
                        "enum": [
                          "normal",
                          "thought",
                          "shout",
                          "whisper",
                          "narration",
                          "system"
                        ]
                      },
                      "font": {
                        "default": "Comic Neue",
                        "type": "string"
                      },
                      "fontSize": {
                        "default": 28,
                        "type": "number",
                        "minimum": 4,
                        "maximum": 200
                      },
                      "lineHeight": {
                        "default": 1.2,
                        "type": "number",
                        "minimum": 0.8,
                        "maximum": 3
                      },
                      "align": {
                        "default": "center",
                        "type": "string",
                        "enum": [
                          "left",
                          "center",
                          "right"
                        ]
                      },
                      "padding": {
                        "default": 18,
                        "type": "number",
                        "minimum": 0,
                        "maximum": 200
                      },
                      "background": {
                        "default": "#ffffff",
                        "type": "string"
                      },
                      "textColor": {
                        "default": "#111111",
                        "type": "string"
                      },
                      "borderColor": {
                        "default": "#111111",
                        "type": "string"
                      },
                      "borderWidth": {
                        "default": 3,
                        "type": "number",
                        "minimum": 0,
                        "maximum": 20
                      },
                      "tail": {
                        "default": true,
                        "type": "boolean"
                      },
                      "tailTarget": {
                        "type": "object",
                        "properties": {
                          "x": {
                            "type": "number",
                            "minimum": 0,
                            "maximum": 1
                          },
                          "y": {
                            "type": "number",
                            "minimum": 0,
                            "maximum": 1
                          }
                        },
                        "required": [
                          "x",
                          "y"
                        ]
                      },
                      "rotation": {
                        "default": 0,
                        "type": "number",
                        "minimum": -180,
                        "maximum": 180
                      },
                      "x": {
                        "type": "number",
                        "minimum": 0,
                        "maximum": 1
                      },
                      "y": {
                        "type": "number",
                        "minimum": 0,
                        "maximum": 1
                      },
                      "width": {
                        "type": "number",
                        "exclusiveMinimum": 0,
                        "maximum": 1
                      },
                      "height": {
                        "type": "number",
                        "exclusiveMinimum": 0,
                        "maximum": 1
                      },
                      "zIndex": {
                        "default": 10,
                        "type": "integer",
                        "minimum": -9007199254740991,
                        "maximum": 9007199254740991
                      }
                    },
                    "required": [
                      "x",
                      "y",
                      "width",
                      "height"
                    ]
                  },
                  {
                    "type": "null"
                  }
                ]
              },
              "text": {
                "type": "string",
                "maxLength": 5000
              }
            },
            "required": [
              "id"
            ]
          }
        }
      }
    }
  },
  "required": [
    "action"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {},
  "additionalProperties": {}
}
```

</details>

### manage_lettering

App lettering: text drawn by OpenManga over the artwork (no image calls, nothing spent). letter_from_plan: place the dialogue and SFX the chapter plan kept for each panel of the page. add_dialogue / update_dialogue: a speech bubble (auto-placed without geometry). add_sfx: a vector sound effect. apply_defaults: apply the project's lettering style/fit to a page, chapter or project. clear: remove bubbles, SFX and on-page captions from a page, chapter or project (narration lines and audio are kept). Chapter- or project-wide changes over many panels are sensitive.

- **Scopes:** `panels:write`
- **Sensitivity:** sensitive-write (the most sensitive action; each call is classified by what it does)
- **Idempotent:** no
- **Annotations:** readOnly=false, destructive=true, idempotent=false, openWorld=false
- **Approval action keys:** `lettering.from_plan`, `lettering.edit`, `lettering.apply_defaults`, `lettering.clear`
- **Wraps:** `POST /api/pages/:id/letter-from-plan`, `POST /api/pages/:id/dialogue`, `PATCH /api/dialogue/:id`, `POST /api/pages/:id/sfx`, `POST /api/pages/:id/lettering/apply-defaults`, `POST /api/pages/:id/lettering/clear`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "action": {
      "type": "string",
      "enum": [
        "letter_from_plan",
        "add_dialogue",
        "update_dialogue",
        "add_sfx",
        "apply_defaults",
        "clear"
      ]
    },
    "pageId": {
      "description": "Every action except update_dialogue.",
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "dialogueId": {
      "description": "For update_dialogue.",
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "dialogue": {
      "type": "object",
      "properties": {
        "panelId": {
          "default": null,
          "anyOf": [
            {
              "type": "string",
              "format": "uuid",
              "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
            },
            {
              "type": "null"
            }
          ]
        },
        "characterId": {
          "default": null,
          "anyOf": [
            {
              "type": "string",
              "format": "uuid",
              "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
            },
            {
              "type": "null"
            }
          ]
        },
        "text": {
          "type": "string",
          "minLength": 1,
          "maxLength": 2000
        },
        "bubble": {
          "type": "object",
          "properties": {
            "type": {
              "default": "normal",
              "type": "string",
              "enum": [
                "normal",
                "thought",
                "shout",
                "whisper",
                "narration",
                "system"
              ]
            },
            "font": {
              "default": "Comic Neue",
              "type": "string"
            },
            "fontSize": {
              "default": 28,
              "type": "number",
              "minimum": 4,
              "maximum": 200
            },
            "lineHeight": {
              "default": 1.2,
              "type": "number",
              "minimum": 0.8,
              "maximum": 3
            },
            "align": {
              "default": "center",
              "type": "string",
              "enum": [
                "left",
                "center",
                "right"
              ]
            },
            "padding": {
              "default": 18,
              "type": "number",
              "minimum": 0,
              "maximum": 200
            },
            "background": {
              "default": "#ffffff",
              "type": "string"
            },
            "textColor": {
              "default": "#111111",
              "type": "string"
            },
            "borderColor": {
              "default": "#111111",
              "type": "string"
            },
            "borderWidth": {
              "default": 3,
              "type": "number",
              "minimum": 0,
              "maximum": 20
            },
            "tail": {
              "default": true,
              "type": "boolean"
            },
            "tailTarget": {
              "type": "object",
              "properties": {
                "x": {
                  "type": "number",
                  "minimum": 0,
                  "maximum": 1
                },
                "y": {
                  "type": "number",
                  "minimum": 0,
                  "maximum": 1
                }
              },
              "required": [
                "x",
                "y"
              ]
            },
            "rotation": {
              "default": 0,
              "type": "number",
              "minimum": -180,
              "maximum": 180
            },
            "x": {
              "type": "number",
              "minimum": 0,
              "maximum": 1
            },
            "y": {
              "type": "number",
              "minimum": 0,
              "maximum": 1
            },
            "width": {
              "type": "number",
              "exclusiveMinimum": 0,
              "maximum": 1
            },
            "height": {
              "type": "number",
              "exclusiveMinimum": 0,
              "maximum": 1
            },
            "zIndex": {
              "default": 10,
              "type": "integer",
              "minimum": -9007199254740991,
              "maximum": 9007199254740991
            }
          },
          "required": [
            "x",
            "y",
            "width",
            "height"
          ]
        },
        "type": {
          "default": "normal",
          "type": "string",
          "enum": [
            "normal",
            "thought",
            "shout",
            "whisper",
            "narration",
            "system"
          ]
        }
      },
      "required": [
        "text"
      ]
    },
    "dialogueUpdate": {
      "type": "object",
      "properties": {
        "text": {
          "type": "string",
          "maxLength": 2000
        },
        "bubble": {
          "type": "object",
          "properties": {
            "type": {
              "default": "normal",
              "type": "string",
              "enum": [
                "normal",
                "thought",
                "shout",
                "whisper",
                "narration",
                "system"
              ]
            },
            "font": {
              "default": "Comic Neue",
              "type": "string"
            },
            "fontSize": {
              "default": 28,
              "type": "number",
              "minimum": 4,
              "maximum": 200
            },
            "lineHeight": {
              "default": 1.2,
              "type": "number",
              "minimum": 0.8,
              "maximum": 3
            },
            "align": {
              "default": "center",
              "type": "string",
              "enum": [
                "left",
                "center",
                "right"
              ]
            },
            "padding": {
              "default": 18,
              "type": "number",
              "minimum": 0,
              "maximum": 200
            },
            "background": {
              "default": "#ffffff",
              "type": "string"
            },
            "textColor": {
              "default": "#111111",
              "type": "string"
            },
            "borderColor": {
              "default": "#111111",
              "type": "string"
            },
            "borderWidth": {
              "default": 3,
              "type": "number",
              "minimum": 0,
              "maximum": 20
            },
            "tail": {
              "default": true,
              "type": "boolean"
            },
            "tailTarget": {
              "type": "object",
              "properties": {
                "x": {
                  "type": "number",
                  "minimum": 0,
                  "maximum": 1
                },
                "y": {
                  "type": "number",
                  "minimum": 0,
                  "maximum": 1
                }
              },
              "required": [
                "x",
                "y"
              ]
            },
            "rotation": {
              "default": 0,
              "type": "number",
              "minimum": -180,
              "maximum": 180
            },
            "x": {
              "type": "number",
              "minimum": 0,
              "maximum": 1
            },
            "y": {
              "type": "number",
              "minimum": 0,
              "maximum": 1
            },
            "width": {
              "type": "number",
              "exclusiveMinimum": 0,
              "maximum": 1
            },
            "height": {
              "type": "number",
              "exclusiveMinimum": 0,
              "maximum": 1
            },
            "zIndex": {
              "default": 10,
              "type": "integer",
              "minimum": -9007199254740991,
              "maximum": 9007199254740991
            }
          },
          "required": [
            "x",
            "y",
            "width",
            "height"
          ]
        },
        "characterId": {
          "anyOf": [
            {
              "type": "string",
              "format": "uuid",
              "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
            },
            {
              "type": "null"
            }
          ]
        },
        "panelId": {
          "anyOf": [
            {
              "type": "string",
              "format": "uuid",
              "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
            },
            {
              "type": "null"
            }
          ]
        },
        "order": {
          "type": "integer",
          "minimum": -9007199254740991,
          "maximum": 9007199254740991
        }
      }
    },
    "sfx": {
      "type": "object",
      "properties": {
        "panelId": {
          "default": null,
          "anyOf": [
            {
              "type": "string",
              "format": "uuid",
              "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
            },
            {
              "type": "null"
            }
          ]
        },
        "text": {
          "type": "string",
          "minLength": 1,
          "maxLength": 100
        },
        "style": {
          "type": "object",
          "properties": {
            "font": {
              "type": "string"
            },
            "fontSize": {
              "type": "number",
              "minimum": 8,
              "maximum": 400
            },
            "fill": {
              "type": "string"
            },
            "stroke": {
              "type": "string"
            },
            "strokeWidth": {
              "type": "number",
              "minimum": 0,
              "maximum": 30
            },
            "rotation": {
              "type": "number",
              "minimum": -180,
              "maximum": 180
            },
            "scale": {
              "type": "number",
              "minimum": 0.1,
              "maximum": 10
            },
            "opacity": {
              "type": "number",
              "minimum": 0,
              "maximum": 1
            },
            "x": {
              "type": "number",
              "minimum": 0,
              "maximum": 1
            },
            "y": {
              "type": "number",
              "minimum": 0,
              "maximum": 1
            },
            "zIndex": {
              "type": "integer",
              "minimum": -9007199254740991,
              "maximum": 9007199254740991
            }
          }
        }
      },
      "required": [
        "text"
      ]
    },
    "applyDefaults": {
      "type": "object",
      "properties": {
        "scope": {
          "default": "page",
          "type": "string",
          "enum": [
            "page",
            "chapter",
            "project"
          ]
        },
        "types": {
          "minItems": 1,
          "type": "array",
          "items": {
            "type": "string",
            "enum": [
              "normal",
              "thought",
              "shout",
              "whisper",
              "narration",
              "system",
              "sfx"
            ]
          }
        },
        "restyle": {
          "default": true,
          "type": "boolean"
        },
        "fit": {
          "default": true,
          "type": "boolean"
        }
      }
    },
    "scope": {
      "default": "page",
      "description": "For clear.",
      "type": "string",
      "enum": [
        "page",
        "chapter",
        "project"
      ]
    }
  },
  "required": [
    "action"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {},
  "additionalProperties": {}
}
```

</details>

### get_panel

One panel: fields (shot, camera, story beat, cast/location/prop versions, prompt override, approval), its current PanelSpec (full history with includeSpecHistory), artwork versions, recent jobs and the cast's names. Read-only.

- **Scopes:** `panels:read`
- **Sensitivity:** read
- **Idempotent:** yes
- **Annotations:** readOnly=true, destructive=false, idempotent=true, openWorld=false

- **Wraps:** `GET /api/panels/:id`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "panelId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "includeSpecHistory": {
      "default": false,
      "type": "boolean"
    }
  },
  "required": [
    "panelId"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {},
  "additionalProperties": {}
}
```

</details>

### update_panel

Edit a panel: shot type, camera angle, story beat, cast (characterVersionIds), location and prop versions, prompt override, frame, scene, approval status; and/or save a new PanelSpec version (`spec`: the whole spec object, shaped like get_panel specs[0].spec). Locked panels are read-only. Setting approvalStatus to approved/locked is sensitive. Nothing is generated; use generate_panel for artwork.

- **Scopes:** `panels:write`
- **Sensitivity:** sensitive-write (the most sensitive action; each call is classified by what it does)
- **Idempotent:** yes
- **Annotations:** readOnly=false, destructive=true, idempotent=true, openWorld=false
- **Approval action keys:** `panel.update`, `panel.approve`
- **Wraps:** `PATCH /api/panels/:id`, `PUT /api/panels/:id/spec`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "frame": {
      "type": "object",
      "properties": {
        "x": {
          "type": "number",
          "minimum": 0,
          "maximum": 1
        },
        "y": {
          "type": "number",
          "minimum": 0,
          "maximum": 1
        },
        "width": {
          "type": "number",
          "exclusiveMinimum": 0,
          "maximum": 1
        },
        "height": {
          "type": "number",
          "exclusiveMinimum": 0,
          "maximum": 1
        }
      },
      "required": [
        "x",
        "y",
        "width",
        "height"
      ]
    },
    "imageTransform": {
      "type": "object",
      "properties": {
        "focalX": {
          "default": 0.5,
          "type": "number",
          "minimum": 0,
          "maximum": 1
        },
        "focalY": {
          "default": 0.5,
          "type": "number",
          "minimum": 0,
          "maximum": 1
        },
        "scale": {
          "default": 1,
          "type": "number",
          "minimum": 1,
          "maximum": 8
        }
      }
    },
    "shotType": {
      "type": "string",
      "enum": [
        "extreme-wide",
        "wide",
        "full",
        "medium",
        "medium-close",
        "close",
        "extreme-close",
        "insert"
      ]
    },
    "cameraAngle": {
      "anyOf": [
        {
          "type": "string",
          "enum": [
            "eye-level",
            "low",
            "high",
            "birds-eye",
            "worms-eye",
            "over-shoulder",
            "dutch",
            "pov"
          ]
        },
        {
          "type": "null"
        }
      ]
    },
    "storyBeat": {
      "type": "string",
      "maxLength": 2000
    },
    "locationVersionId": {
      "anyOf": [
        {
          "type": "string",
          "format": "uuid",
          "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
        },
        {
          "type": "null"
        }
      ]
    },
    "characterVersionIds": {
      "maxItems": 12,
      "type": "array",
      "items": {
        "type": "string",
        "format": "uuid",
        "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
      }
    },
    "propVersionIds": {
      "maxItems": 12,
      "type": "array",
      "items": {
        "type": "string",
        "format": "uuid",
        "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
      }
    },
    "promptOverride": {
      "anyOf": [
        {
          "type": "string",
          "maxLength": 32000
        },
        {
          "type": "null"
        }
      ]
    },
    "seam": {
      "anyOf": [
        {
          "type": "object",
          "properties": {
            "kind": {
              "default": "gap",
              "type": "string",
              "enum": [
                "gap",
                "butt",
                "bleed",
                "dissolve",
                "fade"
              ]
            },
            "size": {
              "type": "integer",
              "minimum": 0,
              "maximum": 2000
            },
            "color": {
              "type": "string",
              "pattern": "^#[0-9a-fA-F]{6}$"
            }
          }
        },
        {
          "type": "null"
        }
      ]
    },
    "promptDraft": {
      "type": "null"
    },
    "approvalStatus": {
      "type": "string",
      "enum": [
        "draft",
        "approved",
        "locked",
        "superseded"
      ]
    },
    "sceneId": {
      "anyOf": [
        {
          "type": "string",
          "format": "uuid",
          "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
        },
        {
          "type": "null"
        }
      ]
    },
    "panelId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "spec": {
      "description": "A complete PanelSpec to save as a new spec version.",
      "type": "object",
      "propertyNames": {
        "type": "string"
      },
      "additionalProperties": {}
    }
  },
  "required": [
    "panelId"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {},
  "additionalProperties": {}
}
```

</details>

### manage_panel_outfits

get: what each character on the panel wears, where that was decided, and the outfits they could wear. set: dress a character in an outfit from this panel onward (until the next change, across chapters) or on this panel only. remove: delete an outfit change (assignmentId); the panels it covered fall back to the previous one.

- **Scopes:** `panels:read`, `panels:write` — per action, see description
- **Sensitivity:** write (the most sensitive action; each call is classified by what it does)
- **Idempotent:** yes
- **Annotations:** readOnly=false, destructive=false, idempotent=true, openWorld=false
- **Approval action keys:** `panel.outfit`
- **Wraps:** `GET /api/panels/:id/outfits`, `PUT /api/panels/:id/outfits`, `DELETE /api/outfit-assignments/:id`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "action": {
      "type": "string",
      "enum": [
        "get",
        "set",
        "remove"
      ]
    },
    "panelId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "set": {
      "type": "object",
      "properties": {
        "characterId": {
          "type": "string",
          "format": "uuid",
          "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
        },
        "outfitId": {
          "type": "string",
          "format": "uuid",
          "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
        },
        "scope": {
          "type": "string",
          "enum": [
            "onward",
            "panel"
          ]
        }
      },
      "required": [
        "characterId",
        "outfitId",
        "scope"
      ]
    },
    "assignmentId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    }
  },
  "required": [
    "action"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {},
  "additionalProperties": {}
}
```

</details>

### get_panel_prompt

The image prompt a panel would be generated with and its reference plan (which character/location/prop references are sent, at what size), without generating anything. Optionally for a specific saved key/model. Read-only.

- **Scopes:** `panels:read`
- **Sensitivity:** read
- **Idempotent:** yes
- **Annotations:** readOnly=true, destructive=false, idempotent=true, openWorld=false

- **Wraps:** `GET /api/panels/:id/prompt-preview`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "panelId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "credentialId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "model": {
      "type": "string",
      "maxLength": 200
    }
  },
  "required": [
    "panelId"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {},
  "additionalProperties": {}
}
```

</details>

### prepare_page_prompts

Queue text-model preparation of the image prompts for every panel on a page (a PanelPromptDraft). Asynchronous: returns a job; poll get_job. Manual mode (ai.manual=true) asks you for the PanelPromptDraft via get_manual_prompt / submit_manual_answer; a provider run spends credits (may need approval).

- **Scopes:** `panels:write`, `generations:run`
- **Sensitivity:** spend (the most sensitive action; each call is classified by what it does)
- **Idempotent:** no — accepts `idempotencyKey`
- **Annotations:** readOnly=false, destructive=false, idempotent=false, openWorld=false
- **Approval action keys:** `page.prepare_prompts`
- **Wraps:** `POST /api/pages/:id/prepare-prompts`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "pageId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "ai": {
      "type": "object",
      "properties": {
        "manual": {
          "description": "Paste mode: the job compiles its prompt and waits for your answer (get_manual_prompt). No spending.",
          "type": "boolean"
        },
        "credentialId": {
          "description": "One of the user's saved provider keys (ids from get_server_info).",
          "type": "string",
          "format": "uuid",
          "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
        },
        "provider": {
          "description": "Use the user's first saved key for this provider kind.",
          "type": "string",
          "maxLength": 40
        },
        "model": {
          "description": "Model id; defaults to the provider's first model.",
          "type": "string",
          "maxLength": 200
        }
      }
    },
    "batch": {
      "type": "boolean"
    },
    "idempotencyKey": {
      "description": "Optional client request id. Retrying with the same key and arguments returns the first result instead of repeating the action; the same key with different arguments is a conflict.",
      "type": "string",
      "minLength": 8,
      "maxLength": 128,
      "pattern": "^[\\w.:-]+$"
    }
  },
  "required": [
    "pageId"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "job": {
      "type": "object",
      "properties": {},
      "additionalProperties": {}
    }
  },
  "required": [
    "job"
  ]
}
```

</details>

### generate_panel

Queue artwork generation for one panel. Always creates a new artwork version (older ones are kept) and spends the user's image-provider credits (may need approval). operation: same_prompt, or a regeneration variant with an instruction. Returns a generation job immediately; poll get_job. For many panels use estimate_bulk_generation + run_bulk_generation.

- **Scopes:** `generations:run`
- **Sensitivity:** spend (the most sensitive action; each call is classified by what it does)
- **Idempotent:** no — accepts `idempotencyKey`
- **Annotations:** readOnly=false, destructive=false, idempotent=false, openWorld=false
- **Approval action keys:** `panel.generate`
- **Wraps:** `POST /api/panels/:id/generate`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "operation": {
      "default": "same_prompt",
      "type": "string",
      "enum": [
        "same_prompt",
        "edited_prompt",
        "change_expression",
        "change_pose",
        "change_camera",
        "change_background",
        "change_outfit",
        "remove_object",
        "add_object",
        "reframe"
      ]
    },
    "instruction": {
      "type": "string",
      "maxLength": 2000
    },
    "promptOverride": {
      "type": "string",
      "maxLength": 32000
    },
    "panelId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "ai": {
      "type": "object",
      "properties": {
        "credentialId": {
          "description": "One of the user's saved provider keys (ids from get_server_info).",
          "type": "string",
          "format": "uuid",
          "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
        },
        "provider": {
          "description": "Use the user's first saved key for this provider kind.",
          "type": "string",
          "maxLength": 40
        },
        "model": {
          "type": "string",
          "maxLength": 200
        }
      }
    },
    "idempotencyKey": {
      "description": "Optional client request id. Retrying with the same key and arguments returns the first result instead of repeating the action; the same key with different arguments is a conflict.",
      "type": "string",
      "minLength": 8,
      "maxLength": 128,
      "pattern": "^[\\w.:-]+$"
    }
  },
  "required": [
    "panelId"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "job": {
      "type": "object",
      "properties": {},
      "additionalProperties": {}
    }
  },
  "required": [
    "job"
  ]
}
```

</details>

### manage_panel_artwork

list: a panel's artwork versions. activate: make an earlier version the panel's active artwork (sensitive). trash: move an old, inactive version to trash (delete class). Masked edits and uploading your own artwork need the OpenManga UI.

- **Scopes:** `panels:read`, `panels:write` — per action, see description
- **Sensitivity:** delete (the most sensitive action; each call is classified by what it does)
- **Idempotent:** yes
- **Annotations:** readOnly=false, destructive=true, idempotent=true, openWorld=false
- **Approval action keys:** `panel.artwork_activate`, `panel.artwork_trash`
- **Wraps:** `GET /api/panels/:id/versions`, `POST /api/panels/:id/versions/:assetId/activate`, `DELETE /api/panels/:id/versions/:assetId`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "action": {
      "type": "string",
      "enum": [
        "list",
        "activate",
        "trash"
      ]
    },
    "panelId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "assetId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    }
  },
  "required": [
    "action",
    "panelId"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {},
  "additionalProperties": {}
}
```

</details>

### run_panel_check

Queue a vision consistency check of a panel's active artwork (expected cast and headcount; a PanelCheck). Asynchronous: returns a job; poll get_job. Manual mode shows you the image in get_manual_prompt and asks for the PanelCheck answer; a provider run spends credits (may need approval). dismiss_review instead clears the panel's review flag.

- **Scopes:** `panels:write`, `generations:run` — per action, see description
- **Sensitivity:** spend (the most sensitive action; each call is classified by what it does)
- **Idempotent:** no
- **Annotations:** readOnly=false, destructive=false, idempotent=false, openWorld=false
- **Approval action keys:** `panel.check`, `panel.review_dismiss`
- **Wraps:** `POST /api/panels/:id/check`, `POST /api/panels/:id/review/dismiss`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "panelId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "action": {
      "default": "check",
      "type": "string",
      "enum": [
        "check",
        "dismiss_review"
      ]
    },
    "ai": {
      "type": "object",
      "properties": {
        "manual": {
          "description": "Paste mode: the job compiles its prompt and waits for your answer (get_manual_prompt). No spending.",
          "type": "boolean"
        },
        "credentialId": {
          "description": "One of the user's saved provider keys (ids from get_server_info).",
          "type": "string",
          "format": "uuid",
          "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
        },
        "provider": {
          "description": "Use the user's first saved key for this provider kind.",
          "type": "string",
          "maxLength": 40
        },
        "model": {
          "description": "Model id; defaults to the provider's first model.",
          "type": "string",
          "maxLength": 200
        }
      }
    },
    "batch": {
      "type": "boolean"
    }
  },
  "required": [
    "panelId"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {},
  "additionalProperties": {}
}
```

</details>

### manage_panel

split: split a panel into two (horizontal or vertical). delete: remove a panel (delete class, may need approval).

- **Scopes:** `panels:write`
- **Sensitivity:** delete (the most sensitive action; each call is classified by what it does)
- **Idempotent:** no
- **Annotations:** readOnly=false, destructive=true, idempotent=false, openWorld=false
- **Approval action keys:** `panel.split`, `panel.delete`
- **Wraps:** `POST /api/panels/:id/split`, `DELETE /api/panels/:id`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "action": {
      "type": "string",
      "enum": [
        "split",
        "delete"
      ]
    },
    "panelId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "direction": {
      "type": "string",
      "enum": [
        "horizontal",
        "vertical"
      ]
    }
  },
  "required": [
    "action",
    "panelId"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {},
  "additionalProperties": {}
}
```

</details>

### get_image

Look at an image itself, returned as image content (not just its id): a panel's current artwork (kind=panel), a page as readers see it, with its speech bubbles, SFX and captions composited (kind=page), a character/location/prop/style reference image (kind=reference, the reference id), or any image asset of a project (kind=asset). size: thumbnail (384 px), preview (1024 px, default) or large (2048 px). Use it to check artwork, consistency or lettering before deciding what to change. Read-only; nothing is generated or spent.

- **Scopes:** `panels:read`, `library:read`, `projects:read` — per action, see description
- **Sensitivity:** read
- **Idempotent:** yes
- **Annotations:** readOnly=true, destructive=false, idempotent=true, openWorld=false

- **Wraps:** `GET /api/panels/:id`, `GET /api/pages/:id/render.png`, `GET /api/assets/:id`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "kind": {
      "type": "string",
      "enum": [
        "panel",
        "page",
        "reference",
        "asset"
      ]
    },
    "id": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$",
      "description": "The panel, page, reference or asset id."
    },
    "size": {
      "default": "preview",
      "type": "string",
      "enum": [
        "thumbnail",
        "preview",
        "large"
      ]
    }
  },
  "required": [
    "kind",
    "id"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "kind": {
      "type": "string"
    },
    "id": {
      "type": "string"
    },
    "mimeType": {
      "type": "string"
    },
    "width": {
      "type": [
        "number",
        "null"
      ]
    },
    "height": {
      "type": [
        "number",
        "null"
      ]
    },
    "bytes": {
      "type": "number"
    }
  },
  "required": [
    "kind",
    "id",
    "mimeType",
    "width",
    "height",
    "bytes"
  ],
  "additionalProperties": {}
}
```

</details>

### list_jobs

A project's AI generation jobs, newest first, with status counts. Filter by status (comma-separated: queued, awaiting_input, processing, completed, failed, cancelled), kind, targetId or batchId. Paginate with `cursor` (nextCursor from the previous page). Prompts are not included (get_manual_prompt / get_job detail). Read-only.

- **Scopes:** `generations:read`
- **Sensitivity:** read
- **Idempotent:** yes
- **Annotations:** readOnly=true, destructive=false, idempotent=true, openWorld=false

- **Wraps:** `GET /api/projects/:projectId/generations`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "projectId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "status": {
      "type": "string",
      "maxLength": 100
    },
    "kind": {
      "type": "string",
      "maxLength": 100
    },
    "targetId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "batchId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "limit": {
      "default": 25,
      "type": "integer",
      "minimum": 1,
      "maximum": 100
    },
    "cursor": {
      "type": "string",
      "maxLength": 100
    }
  },
  "required": [
    "projectId"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "jobs": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {},
        "additionalProperties": {}
      }
    },
    "counts": {
      "type": "object",
      "properties": {},
      "additionalProperties": {}
    },
    "nextCursor": {
      "type": [
        "string",
        "null"
      ]
    }
  },
  "required": [
    "jobs",
    "counts",
    "nextCursor"
  ]
}
```

</details>

### get_job

Poll any job by id: generation (text or image), narration audio, or export/import. status: queued/submitted/processing = still running (poll again after a few seconds, not in a tight loop); awaiting_input = a manual job waiting for your answer (get_manual_prompt); completed/failed/cancelled = final. Completed exports include their files. detail=true adds a generation's inputs, outputs, usage and cost. Read-only.

- **Scopes:** `generations:read`
- **Sensitivity:** read
- **Idempotent:** yes
- **Annotations:** readOnly=true, destructive=false, idempotent=true, openWorld=false

- **Wraps:** `GET /api/jobs/:id`, `GET /api/generations/:id`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "jobId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "detail": {
      "default": false,
      "type": "boolean"
    }
  },
  "required": [
    "jobId"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "type": {
      "type": "string"
    },
    "job": {
      "type": "object",
      "properties": {},
      "additionalProperties": {}
    }
  },
  "required": [
    "type",
    "job"
  ],
  "additionalProperties": {}
}
```

</details>

### get_manual_prompt

For a manual (paste-mode) job that is awaiting_input: the exact compiled prompt a provider would have been sent, the answer format for this question (a commented interface with every field), a valid example answer, how many answers were given so far, the last validation error if the previous answer was rejected, and the images the question is about (as image content). Write your answer as ONE JSON object matching the format and send it with submit_manual_answer. Read-only.

- **Scopes:** `generations:read`
- **Sensitivity:** read
- **Idempotent:** yes
- **Annotations:** readOnly=true, destructive=false, idempotent=true, openWorld=false

- **Wraps:** `GET /api/generations/:id/manual`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "jobId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "includeImages": {
      "default": true,
      "type": "boolean"
    }
  },
  "required": [
    "jobId"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {},
  "additionalProperties": {}
}
```

</details>

### submit_manual_answer

Answer the question a manual (paste-mode) job is waiting for. OpenManga validates it exactly as a provider's reply (schema and the job's own checks) and applies it. Asynchronous: the job is requeued; poll get_job. It then completes, asks the next question (awaiting_input again — a chapter plan asks one per scene), or goes back to awaiting_input with lastError when the answer was rejected: fix only what the error names and resubmit. No provider is used and nothing is spent.

- **Scopes:** `generations:run`
- **Sensitivity:** write (the most sensitive action; each call is classified by what it does)
- **Idempotent:** no
- **Annotations:** readOnly=false, destructive=false, idempotent=false, openWorld=false
- **Approval action keys:** `generation.manual_answer`
- **Wraps:** `POST /api/generations/:id/manual`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "jobId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "answer": {
      "anyOf": [
        {
          "type": "string",
          "minLength": 1,
          "maxLength": 2000000
        },
        {
          "type": "object",
          "propertyNames": {
            "type": "string"
          },
          "additionalProperties": {}
        }
      ],
      "description": "The answer JSON, as an object or as its text."
    }
  },
  "required": [
    "jobId",
    "answer"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "accepted": {
      "type": "boolean"
    },
    "jobId": {
      "type": "string"
    }
  },
  "required": [
    "accepted",
    "jobId"
  ]
}
```

</details>

### control_job

cancel: stop a queued job (a running one is asked to stop). Works for generation, narration audio and export jobs. retry: run a failed or cancelled generation again as a NEW job (follow the returned job); retrying a provider job spends credits again (may need approval), retrying a manual job does not.

- **Scopes:** `generations:run`
- **Sensitivity:** spend (the most sensitive action; each call is classified by what it does)
- **Idempotent:** no — accepts `idempotencyKey`
- **Annotations:** readOnly=false, destructive=false, idempotent=false, openWorld=false
- **Approval action keys:** `job.cancel`, `generation.retry`
- **Wraps:** `POST /api/generations/:id/cancel`, `POST /api/generations/:id/retry`, `POST /api/audio-jobs/:id/cancel`, `POST /api/exports/:id/cancel`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "jobId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "action": {
      "type": "string",
      "enum": [
        "cancel",
        "retry"
      ]
    },
    "idempotencyKey": {
      "description": "Optional client request id. Retrying with the same key and arguments returns the first result instead of repeating the action; the same key with different arguments is a conflict.",
      "type": "string",
      "minLength": 8,
      "maxLength": 128,
      "pattern": "^[\\w.:-]+$"
    }
  },
  "required": [
    "jobId",
    "action"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {},
  "additionalProperties": {}
}
```

</details>

### estimate_bulk_generation

Count and price image generation for a page, scene, chapter, list of panels, or every location/prop reference, WITHOUT starting anything: how many would run, how many are skipped and why, estimated USD, the provider/model, the project budget, a preflight report and credential readiness. Returns an estimateToken: pass it to run_bulk_generation to start exactly this. Read-only.

- **Scopes:** `generations:read`
- **Sensitivity:** read
- **Idempotent:** yes
- **Annotations:** readOnly=true, destructive=false, idempotent=true, openWorld=false

- **Wraps:** `POST /api/projects/:projectId/generations/bulk (confirm=false)`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "projectId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "scope": {
      "type": "object",
      "properties": {
        "pageId": {
          "type": "string",
          "format": "uuid",
          "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
        },
        "sceneId": {
          "type": "string",
          "format": "uuid",
          "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
        },
        "chapterId": {
          "type": "string",
          "format": "uuid",
          "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
        },
        "panelIds": {
          "maxItems": 500,
          "type": "array",
          "items": {
            "type": "string",
            "format": "uuid",
            "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
          }
        },
        "references": {
          "description": "One reference image for every location or every prop",
          "type": "string",
          "enum": [
            "location",
            "prop"
          ]
        }
      },
      "description": "Exactly one of pageId, sceneId, chapterId, panelIds, references."
    },
    "onlyMissing": {
      "default": true,
      "description": "Skip panels that already have artwork (default true).",
      "type": "boolean"
    },
    "batch": {
      "default": false,
      "description": "Provider batch API: half price, results within 24h.",
      "type": "boolean"
    },
    "ai": {
      "type": "object",
      "properties": {
        "credentialId": {
          "description": "One of the user's saved provider keys (ids from get_server_info).",
          "type": "string",
          "format": "uuid",
          "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
        },
        "provider": {
          "description": "Use the user's first saved key for this provider kind.",
          "type": "string",
          "maxLength": 40
        },
        "model": {
          "type": "string",
          "maxLength": 200
        }
      }
    }
  },
  "required": [
    "projectId",
    "scope"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "count": {
      "type": "number"
    },
    "estimatedUsd": {
      "type": [
        "number",
        "null"
      ]
    },
    "estimateToken": {
      "type": "string"
    }
  },
  "required": [
    "count",
    "estimatedUsd",
    "estimateToken"
  ],
  "additionalProperties": {}
}
```

</details>

### run_bulk_generation

Start the image generation you estimated with estimate_bulk_generation (same arguments plus its estimateToken). Spends the user's image-provider credits: always a spend action (may need approval). Re-estimates first; if the count, price, provider or model changed it refuses with estimate_changed and the new estimate, so nothing more is spent than was shown. Respects the project budget (402 budget_exceeded); allowOverBudget only when the user explicitly chose to exceed it. Asynchronous: returns a batchId and jobs; follow with manage_batch status or list_jobs.

- **Scopes:** `generations:run`
- **Sensitivity:** spend (the most sensitive action; each call is classified by what it does)
- **Idempotent:** no — accepts `idempotencyKey`
- **Annotations:** readOnly=false, destructive=false, idempotent=false, openWorld=false
- **Approval action keys:** `generation.bulk`, `generation.bulk_over_budget`
- **Wraps:** `POST /api/projects/:projectId/generations/bulk (confirm=true)`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "projectId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "scope": {
      "type": "object",
      "properties": {
        "pageId": {
          "type": "string",
          "format": "uuid",
          "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
        },
        "sceneId": {
          "type": "string",
          "format": "uuid",
          "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
        },
        "chapterId": {
          "type": "string",
          "format": "uuid",
          "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
        },
        "panelIds": {
          "maxItems": 500,
          "type": "array",
          "items": {
            "type": "string",
            "format": "uuid",
            "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
          }
        },
        "references": {
          "description": "One reference image for every location or every prop",
          "type": "string",
          "enum": [
            "location",
            "prop"
          ]
        }
      },
      "description": "Exactly one of pageId, sceneId, chapterId, panelIds, references."
    },
    "onlyMissing": {
      "default": true,
      "description": "Skip panels that already have artwork (default true).",
      "type": "boolean"
    },
    "batch": {
      "default": false,
      "description": "Provider batch API: half price, results within 24h.",
      "type": "boolean"
    },
    "ai": {
      "type": "object",
      "properties": {
        "credentialId": {
          "description": "One of the user's saved provider keys (ids from get_server_info).",
          "type": "string",
          "format": "uuid",
          "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
        },
        "provider": {
          "description": "Use the user's first saved key for this provider kind.",
          "type": "string",
          "maxLength": 40
        },
        "model": {
          "type": "string",
          "maxLength": 200
        }
      }
    },
    "estimateToken": {
      "type": "string",
      "minLength": 8,
      "maxLength": 64
    },
    "allowOverBudget": {
      "default": false,
      "description": "Only when the user explicitly chose to go over the project budget.",
      "type": "boolean"
    },
    "idempotencyKey": {
      "description": "Optional client request id. Retrying with the same key and arguments returns the first result instead of repeating the action; the same key with different arguments is a conflict.",
      "type": "string",
      "minLength": 8,
      "maxLength": 128,
      "pattern": "^[\\w.:-]+$"
    }
  },
  "required": [
    "projectId",
    "scope",
    "estimateToken"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "batchId": {
      "type": [
        "string",
        "null"
      ]
    },
    "jobs": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {},
        "additionalProperties": {}
      }
    }
  },
  "required": [
    "batchId",
    "jobs"
  ],
  "additionalProperties": {}
}
```

</details>

### manage_batch

list: a project's active and recent bulk batches (needs projectId). status: one batch's progress. pause: not-yet-started jobs stop, running ones finish. resume: continue a paused batch (resumes spending; may need approval). poll: ask the worker to check provider batches now. cancel: cancel all not-yet-started jobs of the batch (sensitive when many would be lost).

- **Scopes:** `generations:read`, `generations:run` — per action, see description
- **Sensitivity:** spend (the most sensitive action; each call is classified by what it does)
- **Idempotent:** no
- **Annotations:** readOnly=false, destructive=false, idempotent=false, openWorld=false
- **Approval action keys:** `batch.pause`, `batch.resume`, `batch.poll`, `batch.cancel`
- **Wraps:** `GET /api/projects/:projectId/generations/batches`, `GET /api/generations/batches/:batchId`, `POST /api/generations/batches/:batchId/pause`, `POST /api/generations/batches/:batchId/resume`, `POST /api/generations/batches/:batchId/poll`, `POST /api/generations/batches/:batchId/cancel`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "action": {
      "type": "string",
      "enum": [
        "list",
        "status",
        "pause",
        "resume",
        "poll",
        "cancel"
      ]
    },
    "projectId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "batchId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    }
  },
  "required": [
    "action"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {},
  "additionalProperties": {}
}
```

</details>

### generate_cover

Queue cover artwork for a project (the title is composited by the app, not drawn by the model). Spends image-provider credits (may need approval). Asynchronous: returns a job; poll get_job.

- **Scopes:** `generations:run`
- **Sensitivity:** spend (the most sensitive action; each call is classified by what it does)
- **Idempotent:** no — accepts `idempotencyKey`
- **Annotations:** readOnly=false, destructive=false, idempotent=false, openWorld=false
- **Approval action keys:** `project.cover`
- **Wraps:** `POST /api/projects/:projectId/cover`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "title": {
      "type": "string",
      "maxLength": 200
    },
    "subtitle": {
      "default": "",
      "type": "string",
      "maxLength": 300
    },
    "composition": {
      "default": "",
      "type": "string",
      "maxLength": 2000
    },
    "characterIds": {
      "default": [],
      "maxItems": 6,
      "type": "array",
      "items": {
        "type": "string",
        "format": "uuid",
        "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
      }
    },
    "projectId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "ai": {
      "type": "object",
      "properties": {
        "credentialId": {
          "description": "One of the user's saved provider keys (ids from get_server_info).",
          "type": "string",
          "format": "uuid",
          "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
        },
        "provider": {
          "description": "Use the user's first saved key for this provider kind.",
          "type": "string",
          "maxLength": 40
        },
        "model": {
          "type": "string",
          "maxLength": 200
        }
      }
    },
    "idempotencyKey": {
      "description": "Optional client request id. Retrying with the same key and arguments returns the first result instead of repeating the action; the same key with different arguments is a conflict.",
      "type": "string",
      "minLength": 8,
      "maxLength": 128,
      "pattern": "^[\\w.:-]+$"
    }
  },
  "required": [
    "title",
    "projectId"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "job": {
      "type": "object",
      "properties": {},
      "additionalProperties": {}
    }
  },
  "required": [
    "job"
  ],
  "additionalProperties": {}
}
```

</details>

### get_chapter_narration

view=lines: a chapter's narration lines, their TTS segments, audio and job states. view=timeline: the machine-readable timeline (panelId, audioAssetId, startMs, durationMs). Optionally one language. Read-only.

- **Scopes:** `narration:read`
- **Sensitivity:** read
- **Idempotent:** yes
- **Annotations:** readOnly=true, destructive=false, idempotent=true, openWorld=false

- **Wraps:** `GET /api/chapters/:id/narration`, `GET /api/chapters/:id/narration/timeline`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "chapterId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "view": {
      "default": "lines",
      "type": "string",
      "enum": [
        "lines",
        "timeline"
      ]
    },
    "language": {
      "type": "string",
      "maxLength": 16
    }
  },
  "required": [
    "chapterId"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {},
  "additionalProperties": {}
}
```

</details>

### get_narration_status

Synthesis progress for every chapter of a project: segments, how many have audio, what is still running. Read-only.

- **Scopes:** `narration:read`
- **Sensitivity:** read
- **Idempotent:** yes
- **Annotations:** readOnly=true, destructive=false, idempotent=true, openWorld=false

- **Wraps:** `GET /api/projects/:projectId/narration/progress`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "projectId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "language": {
      "type": "string",
      "maxLength": 16
    }
  },
  "required": [
    "projectId"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {},
  "additionalProperties": {}
}
```

</details>

### edit_narration

add_line: add a narration line to a chapter (auto-split into TTS segments), optionally tied to a panel and shown on the page. update_line: edit text (re-segments; unchanged segments keep their audio), panel, on-page box or order. delete_line (delete class). resegment_line: re-split a line. update_segment: text, voice, speed, pause. split_segment / merge_segment. apply_pauses: re-apply the project's pause settings to a chapter. No audio is produced here; use synthesize_narration.

- **Scopes:** `narration:write`
- **Sensitivity:** delete (the most sensitive action; each call is classified by what it does)
- **Idempotent:** no
- **Annotations:** readOnly=false, destructive=true, idempotent=false, openWorld=false
- **Approval action keys:** `narration.edit`, `narration.delete_line`
- **Wraps:** `POST /api/chapters/:id/narration/lines`, `PATCH /api/narration-lines/:id`, `DELETE /api/narration-lines/:id`, `POST /api/narration-lines/:id/resegment`, `PATCH /api/narration-segments/:id`, `POST /api/narration-segments/:id/split`, `POST /api/narration-segments/:id/merge-next`, `POST /api/chapters/:id/narration/pauses`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "action": {
      "type": "string",
      "enum": [
        "add_line",
        "update_line",
        "delete_line",
        "resegment_line",
        "update_segment",
        "split_segment",
        "merge_segment",
        "apply_pauses"
      ]
    },
    "chapterId": {
      "description": "For add_line and apply_pauses.",
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "lineId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "segmentId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "line": {
      "type": "object",
      "properties": {
        "text": {
          "type": "string",
          "minLength": 1,
          "maxLength": 20000
        },
        "language": {
          "type": "string",
          "minLength": 2,
          "maxLength": 16
        },
        "panelId": {
          "default": null,
          "anyOf": [
            {
              "type": "string",
              "format": "uuid",
              "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
            },
            {
              "type": "null"
            }
          ]
        },
        "showOnPage": {
          "default": false,
          "type": "boolean"
        },
        "afterLineId": {
          "type": "string",
          "format": "uuid",
          "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
        }
      },
      "required": [
        "text"
      ]
    },
    "lineUpdate": {
      "type": "object",
      "properties": {
        "text": {
          "type": "string",
          "minLength": 1,
          "maxLength": 20000
        },
        "panelId": {
          "anyOf": [
            {
              "type": "string",
              "format": "uuid",
              "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
            },
            {
              "type": "null"
            }
          ]
        },
        "showOnPage": {
          "type": "boolean"
        },
        "box": {
          "anyOf": [
            {
              "type": "object",
              "properties": {
                "type": {
                  "default": "normal",
                  "type": "string",
                  "enum": [
                    "normal",
                    "thought",
                    "shout",
                    "whisper",
                    "narration",
                    "system"
                  ]
                },
                "font": {
                  "default": "Comic Neue",
                  "type": "string"
                },
                "fontSize": {
                  "default": 28,
                  "type": "number",
                  "minimum": 4,
                  "maximum": 200
                },
                "lineHeight": {
                  "default": 1.2,
                  "type": "number",
                  "minimum": 0.8,
                  "maximum": 3
                },
                "align": {
                  "default": "center",
                  "type": "string",
                  "enum": [
                    "left",
                    "center",
                    "right"
                  ]
                },
                "padding": {
                  "default": 18,
                  "type": "number",
                  "minimum": 0,
                  "maximum": 200
                },
                "background": {
                  "default": "#ffffff",
                  "type": "string"
                },
                "textColor": {
                  "default": "#111111",
                  "type": "string"
                },
                "borderColor": {
                  "default": "#111111",
                  "type": "string"
                },
                "borderWidth": {
                  "default": 3,
                  "type": "number",
                  "minimum": 0,
                  "maximum": 20
                },
                "tail": {
                  "default": true,
                  "type": "boolean"
                },
                "tailTarget": {
                  "type": "object",
                  "properties": {
                    "x": {
                      "type": "number",
                      "minimum": 0,
                      "maximum": 1
                    },
                    "y": {
                      "type": "number",
                      "minimum": 0,
                      "maximum": 1
                    }
                  },
                  "required": [
                    "x",
                    "y"
                  ]
                },
                "rotation": {
                  "default": 0,
                  "type": "number",
                  "minimum": -180,
                  "maximum": 180
                },
                "x": {
                  "type": "number",
                  "minimum": 0,
                  "maximum": 1
                },
                "y": {
                  "type": "number",
                  "minimum": 0,
                  "maximum": 1
                },
                "width": {
                  "type": "number",
                  "exclusiveMinimum": 0,
                  "maximum": 1
                },
                "height": {
                  "type": "number",
                  "exclusiveMinimum": 0,
                  "maximum": 1
                },
                "zIndex": {
                  "default": 10,
                  "type": "integer",
                  "minimum": -9007199254740991,
                  "maximum": 9007199254740991
                }
              },
              "required": [
                "x",
                "y",
                "width",
                "height"
              ]
            },
            {
              "type": "null"
            }
          ]
        },
        "order": {
          "type": "integer",
          "minimum": 1,
          "maximum": 9007199254740991
        }
      }
    },
    "segmentUpdate": {
      "type": "object",
      "properties": {
        "text": {
          "type": "string",
          "minLength": 1,
          "maxLength": 4000
        },
        "voice": {
          "anyOf": [
            {
              "type": "string",
              "maxLength": 64
            },
            {
              "type": "null"
            }
          ]
        },
        "speed": {
          "anyOf": [
            {
              "type": "number",
              "minimum": 0.5,
              "maximum": 2
            },
            {
              "type": "null"
            }
          ]
        },
        "pauseAfterMs": {
          "type": "integer",
          "minimum": 0,
          "maximum": 10000
        }
      }
    },
    "maxChars": {
      "type": "integer",
      "minimum": 40,
      "maximum": 2000
    },
    "at": {
      "description": "split_segment: character offset.",
      "type": "integer",
      "minimum": 1,
      "maximum": 9007199254740991
    },
    "language": {
      "type": "string",
      "maxLength": 16
    }
  },
  "required": [
    "action"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {},
  "additionalProperties": {}
}
```

</details>

### run_narration_generation

Queue the text model writing narration lines for a chapter, tied to its panels (a NarrationDraft). replace=true replaces existing lines (sensitive). Asynchronous: returns a job; poll get_job. Manual mode (ai.manual=true, no spending) asks you for the NarrationDraft via get_manual_prompt / submit_manual_answer; a provider run spends credits (may need approval). Then synthesize_narration for audio.

- **Scopes:** `narration:write`, `generations:run`
- **Sensitivity:** spend (the most sensitive action; each call is classified by what it does)
- **Idempotent:** no — accepts `idempotencyKey`
- **Annotations:** readOnly=false, destructive=false, idempotent=false, openWorld=false
- **Approval action keys:** `narration.generate`, `narration.regenerate`
- **Wraps:** `POST /api/chapters/:id/narration/generate`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "style": {
      "default": "",
      "type": "string",
      "maxLength": 500
    },
    "replace": {
      "default": false,
      "type": "boolean"
    },
    "wordsPerPanel": {
      "type": "integer",
      "minimum": 5,
      "maximum": 80
    },
    "language": {
      "type": "string",
      "minLength": 2,
      "maxLength": 16
    },
    "chapterId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "ai": {
      "type": "object",
      "properties": {
        "manual": {
          "description": "Paste mode: the job compiles its prompt and waits for your answer (get_manual_prompt). No spending.",
          "type": "boolean"
        },
        "credentialId": {
          "description": "One of the user's saved provider keys (ids from get_server_info).",
          "type": "string",
          "format": "uuid",
          "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
        },
        "provider": {
          "description": "Use the user's first saved key for this provider kind.",
          "type": "string",
          "maxLength": 40
        },
        "model": {
          "description": "Model id; defaults to the provider's first model.",
          "type": "string",
          "maxLength": 200
        }
      }
    },
    "batch": {
      "type": "boolean"
    },
    "idempotencyKey": {
      "description": "Optional client request id. Retrying with the same key and arguments returns the first result instead of repeating the action; the same key with different arguments is a conflict.",
      "type": "string",
      "minLength": 8,
      "maxLength": 128,
      "pattern": "^[\\w.:-]+$"
    }
  },
  "required": [
    "chapterId"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "job": {
      "type": "object",
      "properties": {},
      "additionalProperties": {}
    }
  },
  "required": [
    "job"
  ],
  "additionalProperties": {}
}
```

</details>

### synthesize_narration

Queue speech synthesis for one segment (segmentId) or every missing/stale segment of a chapter (chapterId). Without ai.credentialId it uses the server's local voice (Kokoro; free). With a speech-provider key it spends the user's credits (may need approval). Asynchronous: returns audio jobs; poll get_job or get_narration_status. cancel=true (with chapterId) instead cancels the chapter's not-yet-started synthesis.

- **Scopes:** `narration:write`
- **Sensitivity:** spend (the most sensitive action; each call is classified by what it does)
- **Idempotent:** no — accepts `idempotencyKey`
- **Annotations:** readOnly=false, destructive=false, idempotent=false, openWorld=false
- **Approval action keys:** `narration.synthesize`, `narration.cancel`
- **Wraps:** `POST /api/narration-segments/:id/synthesize`, `POST /api/chapters/:id/narration/synthesize`, `POST /api/chapters/:id/narration/synthesize/cancel`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "segmentId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "chapterId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "voice": {
      "type": "string",
      "maxLength": 128
    },
    "speed": {
      "type": "number",
      "minimum": 0.5,
      "maximum": 2
    },
    "force": {
      "default": false,
      "description": "Segment: re-synthesize even if audio exists.",
      "type": "boolean"
    },
    "onlyMissing": {
      "default": true,
      "description": "Chapter: only segments without current audio.",
      "type": "boolean"
    },
    "language": {
      "type": "string",
      "maxLength": 16
    },
    "ai": {
      "type": "object",
      "properties": {
        "credentialId": {
          "description": "A saved speech-provider key; omit for the server's local voice (free).",
          "type": "string",
          "format": "uuid",
          "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
        },
        "provider": {
          "type": "string",
          "maxLength": 40
        },
        "model": {
          "type": "string",
          "maxLength": 200
        }
      }
    },
    "cancel": {
      "default": false,
      "type": "boolean"
    },
    "idempotencyKey": {
      "description": "Optional client request id. Retrying with the same key and arguments returns the first result instead of repeating the action; the same key with different arguments is a conflict.",
      "type": "string",
      "minLength": 8,
      "maxLength": 128,
      "pattern": "^[\\w.:-]+$"
    }
  }
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {},
  "additionalProperties": {}
}
```

</details>

### create_export

Queue an export job: pages as PNG/JPG, PDF, webtoon strip, ZIP package, project JSON, narration audio, timeline, agent package, or video (pages / panels). Deterministic composition, no AI calls and nothing spent; still treated as sensitive (may need approval). Run get_project_checks check=readiness first; acknowledgeIssues=true exports despite reported issues. Asynchronous: returns the job (not a file); poll get_job until completed, which then lists the files, or list_exports.

- **Scopes:** `exports:create`
- **Sensitivity:** sensitive-write (the most sensitive action; each call is classified by what it does)
- **Idempotent:** no — accepts `idempotencyKey`
- **Annotations:** readOnly=false, destructive=true, idempotent=false, openWorld=false
- **Approval action keys:** `project.export`
- **Wraps:** `POST /api/projects/:projectId/exports`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "kind": {
      "type": "string",
      "enum": [
        "png_pages",
        "jpg_pages",
        "pdf",
        "webtoon",
        "zip_package",
        "project_json",
        "narration_audio",
        "timeline",
        "agent_package",
        "video_pages",
        "video_panels"
      ]
    },
    "chapterId": {
      "default": null,
      "anyOf": [
        {
          "type": "string",
          "format": "uuid",
          "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
        },
        {
          "type": "null"
        }
      ]
    },
    "pageIds": {
      "maxItems": 500,
      "type": "array",
      "items": {
        "type": "string",
        "format": "uuid",
        "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
      }
    },
    "scale": {
      "default": 1,
      "type": "number",
      "minimum": 0.25,
      "maximum": 3
    },
    "jpgQuality": {
      "default": 90,
      "type": "integer",
      "minimum": 40,
      "maximum": 100
    },
    "pdf": {
      "default": {
        "pageSize": "source",
        "marginMm": 0,
        "bleedMm": 0,
        "dpi": 300
      },
      "type": "object",
      "properties": {
        "pageSize": {
          "default": "source",
          "type": "string",
          "enum": [
            "source",
            "A4",
            "A5",
            "B5",
            "letter",
            "tankobon"
          ]
        },
        "marginMm": {
          "default": 0,
          "type": "number",
          "minimum": 0,
          "maximum": 50
        },
        "bleedMm": {
          "default": 0,
          "type": "number",
          "minimum": 0,
          "maximum": 10
        },
        "dpi": {
          "default": 300,
          "type": "integer",
          "minimum": 72,
          "maximum": 600
        },
        "readingDirection": {
          "type": "string",
          "enum": [
            "ltr",
            "rtl",
            "vertical"
          ]
        }
      }
    },
    "webtoon": {
      "default": {
        "split": true,
        "format": "jpg"
      },
      "type": "object",
      "properties": {
        "width": {
          "type": "integer",
          "minimum": 320,
          "maximum": 2000
        },
        "gap": {
          "type": "integer",
          "minimum": 0,
          "maximum": 1000
        },
        "split": {
          "default": true,
          "type": "boolean"
        },
        "maxChunkHeight": {
          "type": "integer",
          "minimum": 1000,
          "maximum": 40000
        },
        "format": {
          "default": "jpg",
          "type": "string",
          "enum": [
            "png",
            "jpg"
          ]
        }
      }
    },
    "audio": {
      "default": {
        "format": "mp3",
        "normalize": true
      },
      "type": "object",
      "properties": {
        "format": {
          "default": "mp3",
          "type": "string",
          "enum": [
            "wav",
            "mp3",
            "ogg"
          ]
        },
        "normalize": {
          "default": true,
          "type": "boolean"
        }
      }
    },
    "includeAssets": {
      "default": true,
      "type": "boolean"
    },
    "video": {
      "default": {
        "height": 1080,
        "fps": 30,
        "minHoldMs": 2500,
        "framing": "width",
        "pageWidthRatio": 0.6,
        "pageHeightRatio": 0.96,
        "maxScrollPxPerSec": 60,
        "zoom": 0.06,
        "breathMs": 150
      },
      "type": "object",
      "properties": {
        "height": {
          "default": 1080,
          "anyOf": [
            {
              "type": "number",
              "const": 720
            },
            {
              "type": "number",
              "const": 1080
            },
            {
              "type": "number",
              "const": 1440
            }
          ]
        },
        "fps": {
          "default": 30,
          "type": "integer",
          "minimum": 12,
          "maximum": 60
        },
        "minHoldMs": {
          "default": 2500,
          "type": "integer",
          "minimum": 500,
          "maximum": 30000
        },
        "framing": {
          "default": "width",
          "type": "string",
          "enum": [
            "width",
            "height"
          ]
        },
        "pageWidthRatio": {
          "default": 0.6,
          "type": "number",
          "minimum": 0.3,
          "maximum": 1
        },
        "pageHeightRatio": {
          "default": 0.96,
          "type": "number",
          "minimum": 0.5,
          "maximum": 1
        },
        "maxScrollPxPerSec": {
          "default": 60,
          "type": "number",
          "minimum": 10,
          "maximum": 400
        },
        "zoom": {
          "default": 0.06,
          "type": "number",
          "minimum": 0,
          "maximum": 0.2
        },
        "breathMs": {
          "default": 150,
          "type": "integer",
          "minimum": 0,
          "maximum": 2000
        }
      }
    },
    "language": {
      "type": "string",
      "minLength": 2,
      "maxLength": 16
    },
    "acknowledgeIssues": {
      "default": false,
      "type": "boolean"
    },
    "projectId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "idempotencyKey": {
      "description": "Optional client request id. Retrying with the same key and arguments returns the first result instead of repeating the action; the same key with different arguments is a conflict.",
      "type": "string",
      "minLength": 8,
      "maxLength": 128,
      "pattern": "^[\\w.:-]+$"
    }
  },
  "required": [
    "kind",
    "projectId"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "job": {
      "type": "object",
      "properties": {},
      "additionalProperties": {}
    }
  },
  "required": [
    "job"
  ],
  "additionalProperties": {}
}
```

</details>

### list_exports

A project's export jobs (newest first) and their downloadable files (file names, sizes, types). Read-only.

- **Scopes:** `exports:read`
- **Sensitivity:** read
- **Idempotent:** yes
- **Annotations:** readOnly=true, destructive=false, idempotent=true, openWorld=false

- **Wraps:** `GET /api/projects/:projectId/exports`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "projectId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "limit": {
      "default": 25,
      "type": "integer",
      "minimum": 1,
      "maximum": 100
    }
  },
  "required": [
    "projectId"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "jobs": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {},
        "additionalProperties": {}
      }
    }
  },
  "required": [
    "jobs"
  ],
  "additionalProperties": {}
}
```

</details>

### list_experts

The experts available to the user (built-in writing/art/story consultants and their own) and, with chats=true, their existing chats (most recent first; chats about projects this connection cannot see are left out). Read-only.

- **Scopes:** `experts:use`
- **Sensitivity:** read
- **Idempotent:** yes
- **Annotations:** readOnly=true, destructive=false, idempotent=true, openWorld=false

- **Wraps:** `GET /api/experts`, `GET /api/expert-chats`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "chats": {
      "default": false,
      "type": "boolean"
    },
    "limit": {
      "default": 25,
      "type": "integer",
      "minimum": 1,
      "maximum": 100
    }
  }
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {},
  "additionalProperties": {}
}
```

</details>

### manage_expert_chat

create: start a chat with an expert (built-in key or own expert id), optionally about a project. get: the chat and its latest messages (paginated from the newest; `before` = number of newest messages to skip). rename. delete (delete class).

- **Scopes:** `experts:use`
- **Sensitivity:** delete (the most sensitive action; each call is classified by what it does)
- **Idempotent:** no
- **Annotations:** readOnly=false, destructive=true, idempotent=false, openWorld=false
- **Approval action keys:** `expert_chat.create`, `expert_chat.rename`, `expert_chat.delete`
- **Wraps:** `POST /api/expert-chats`, `GET /api/expert-chats/:id`, `PATCH /api/expert-chats/:id`, `DELETE /api/expert-chats/:id`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "action": {
      "type": "string",
      "enum": [
        "create",
        "get",
        "rename",
        "delete"
      ]
    },
    "chatId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "create": {
      "type": "object",
      "properties": {
        "expert": {
          "type": "string",
          "minLength": 1,
          "maxLength": 100
        },
        "projectId": {
          "default": null,
          "anyOf": [
            {
              "type": "string",
              "format": "uuid",
              "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "required": [
        "expert"
      ]
    },
    "title": {
      "type": "string",
      "minLength": 1,
      "maxLength": 200
    },
    "limit": {
      "default": 20,
      "type": "integer",
      "minimum": 1,
      "maximum": 100
    },
    "before": {
      "default": 0,
      "type": "integer",
      "minimum": 0,
      "maximum": 9007199254740991
    }
  },
  "required": [
    "action"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {},
  "additionalProperties": {}
}
```

</details>

### send_expert_message

Send a message to an expert chat; the reply is written in the background and appears on the chat (read it with manage_expert_chat get after a few seconds). With ai.manual=true the reply waits for you to paste it (answer_expert_reply); with a provider it spends credits (may need approval). generateImage=true also asks for an image (spends image credits). Image attachments must be uploaded in the OpenManga UI.

- **Scopes:** `experts:use`
- **Sensitivity:** spend (the most sensitive action; each call is classified by what it does)
- **Idempotent:** no
- **Annotations:** readOnly=false, destructive=false, idempotent=false, openWorld=false
- **Approval action keys:** `expert.message`
- **Wraps:** `POST /api/expert-chats/:id/messages`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "chatId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "text": {
      "type": "string",
      "maxLength": 50000
    },
    "generateImage": {
      "default": false,
      "type": "boolean"
    },
    "aspectRatio": {
      "default": 1,
      "type": "number",
      "minimum": 0.25,
      "maximum": 4
    },
    "ai": {
      "type": "object",
      "properties": {
        "manual": {
          "description": "Paste mode: the job compiles its prompt and waits for your answer (get_manual_prompt). No spending.",
          "type": "boolean"
        },
        "credentialId": {
          "description": "One of the user's saved provider keys (ids from get_server_info).",
          "type": "string",
          "format": "uuid",
          "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
        },
        "provider": {
          "description": "Use the user's first saved key for this provider kind.",
          "type": "string",
          "maxLength": 40
        },
        "model": {
          "description": "Model id; defaults to the provider's first model.",
          "type": "string",
          "maxLength": 200
        }
      }
    },
    "imageAi": {
      "type": "object",
      "properties": {
        "credentialId": {
          "description": "One of the user's saved provider keys (ids from get_server_info).",
          "type": "string",
          "format": "uuid",
          "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
        },
        "provider": {
          "description": "Use the user's first saved key for this provider kind.",
          "type": "string",
          "maxLength": 40
        },
        "model": {
          "type": "string",
          "maxLength": 200
        }
      }
    }
  },
  "required": [
    "chatId",
    "text"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {},
  "additionalProperties": {}
}
```

</details>

### answer_expert_reply

Give the text of a reply that is waiting for one (a manual-mode expert message). No provider is used. Returns the updated message.

- **Scopes:** `experts:use`
- **Sensitivity:** write (the most sensitive action; each call is classified by what it does)
- **Idempotent:** no
- **Annotations:** readOnly=false, destructive=false, idempotent=false, openWorld=false
- **Approval action keys:** `expert.answer`
- **Wraps:** `POST /api/expert-messages/:id/answer`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "messageId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "text": {
      "type": "string",
      "minLength": 1,
      "maxLength": 100000
    }
  },
  "required": [
    "messageId",
    "text"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {},
  "additionalProperties": {}
}
```

</details>

### retry_expert_reply

Write the chat's last reply again (after a failure, or for a different answer). A provider run spends credits (may need approval); manual mode does not.

- **Scopes:** `experts:use`
- **Sensitivity:** spend (the most sensitive action; each call is classified by what it does)
- **Idempotent:** no
- **Annotations:** readOnly=false, destructive=false, idempotent=false, openWorld=false
- **Approval action keys:** `expert.retry`
- **Wraps:** `POST /api/expert-chats/:id/retry`

<details><summary>Input schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "chatId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
    },
    "ai": {
      "type": "object",
      "properties": {
        "manual": {
          "description": "Paste mode: the job compiles its prompt and waits for your answer (get_manual_prompt). No spending.",
          "type": "boolean"
        },
        "credentialId": {
          "description": "One of the user's saved provider keys (ids from get_server_info).",
          "type": "string",
          "format": "uuid",
          "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
        },
        "provider": {
          "description": "Use the user's first saved key for this provider kind.",
          "type": "string",
          "maxLength": 40
        },
        "model": {
          "description": "Model id; defaults to the provider's first model.",
          "type": "string",
          "maxLength": 200
        }
      }
    },
    "generateImage": {
      "type": "boolean"
    }
  },
  "required": [
    "chatId"
  ]
}
```

</details>

<details><summary>Output (<code>data</code>) schema</summary>

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {},
  "additionalProperties": {}
}
```

</details>
